% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated-gs.R
\name{run_gsea_analysis}
\alias{run_gsea_analysis}
\title{Deprecated: run a multi-database GSEA analysis and save its plots}
\usage{
run_gsea_analysis(
  de_table,
  analysis_name,
  rank_metric = "t",
  species = "Mus musculus",
  n_pathways = 30,
  padj_cutoff = 0.05,
  save_plots = TRUE,
  output_dir = "./GSEA_Plots",
  databases = NULL,
  nperm = 1e+05,
  pvalue_cutoff = 1,
  sample_annotation = NULL,
  sample_order = NULL,
  helper_root = NULL
)
}
\arguments{
\item{de_table}{Differential expression results table.}

\item{analysis_name}{Name of the analysis, used in plot filenames.}

\item{rank_metric}{Column to rank genes by (default: \code{"t"}).}

\item{species}{Species for MSigDB gene sets (default: \code{"Mus musculus"}).}

\item{n_pathways}{Number of top pathways to display (default: 30).}

\item{padj_cutoff}{Adjusted p-value cutoff for highlighting (default:
0.05).}

\item{save_plots}{Logical. Save generated plots (default: TRUE).}

\item{output_dir}{Directory to save plots.}

\item{databases}{List of database configs (default: NULL, uses the
predefined MSigDB set).}

\item{nperm}{Number of permutations for GSEA (default: 100000).}

\item{pvalue_cutoff}{P-value cutoff for storing GSEA results (default: 1,
stores ALL pathways).}

\item{sample_annotation}{Accepted and ignored (with a warning when
supplied); see Details.}

\item{sample_order}{Accepted and ignored (with a warning when supplied);
see Details.}

\item{helper_root}{Accepted and ignored: the new API needs no
\code{source()}-based helper resolution.}
}
\value{
List of \code{\link[=gs_result]{gs_result()}} objects for each database, invisibly.
}
\description{
Superseded by looping \code{\link[=gs_ranks]{gs_ranks()}}/\code{\link[=gsdb_msigdb]{gsdb_msigdb()}}/\code{\link[=gs_test]{gs_test()}} over the
databases of interest and rendering with \code{\link[=gs_plot_dot]{gs_plot_dot()}}, \code{\link[=gs_plot_bar]{gs_plot_bar()}},
\code{\link[=gs_plot_running]{gs_plot_running()}} and \code{\link[=gs_save]{gs_save()}}. This shim reproduces the old formals
and default database list, and forwards each database's run to the
\code{run_gsea()} shim above (so the \code{ranks}/\code{gene_sets} attributes it sets are
available for the running-sum plot) rather than sourcing helper scripts --
there is no \code{source()} step in the new API.
}
\details{
Simplifications relative to the old pipeline, made deliberately thin per
the module brief: the up/down/facet dotplot triptych collapses to one
\code{\link[=gs_plot_dot]{gs_plot_dot()}} call with \code{direction = "both"}; \code{sample_annotation} and
\code{sample_order} (the per-database sample x pathway heatmap) are accepted but
ignored, with a warning, because \code{\link[=gs_plot_heatmap]{gs_plot_heatmap()}} draws directly from a
\code{\link[=gs_result]{gs_result()}} or \code{\link[=gs_matrix]{gs_matrix()}} and has no equivalent single-NES-vector
knockoff heatmap.
}
