% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-running.R
\name{.grs_windows}
\alias{.grs_windows}
\title{Panel windows: exact panel heights without clipping any data}
\usage{
.grs_windows(ranges, heights)
}
\arguments{
\item{ranges}{Named list of length-2 numeric natural ranges, in panel order.}

\item{heights}{Length-3 numeric ratio.}
}
\value{
A named list, one entry per panel, each \code{list(lo =, hi =, h =)}.
}
\description{
\code{facet_grid(space = "free_y")} sizes a panel in proportion to its y range,
and the natural ranges of an ES curve, a tick lane and a ranked metric have
nothing to do with the proportions a reader wants. So each panel's data is
mapped onto its own window \verb{[0, panel_heights[k]]} -- a linear, invertible
rescaling that clips nothing -- and the panel heights then come out exactly in
the requested ratio. The axis still reads in original units, because
\code{\link[=.grs_break_fun]{.grs_break_fun()}} inverts the map and returns \emph{named} breaks, which ggplot2
uses as the labels.
}
\details{
A 10\% margin is added to the ES and metric ranges here rather than left to
scale expansion, so the plotted window limits are exactly \code{c(0, h)} and each
panel stays identifiable by them.
}
\keyword{internal}
