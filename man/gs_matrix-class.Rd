% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-matrix.R
\name{gs_matrix-class}
\alias{gs_matrix-class}
\title{The \code{gs_matrix} contract}
\description{
\code{gs_matrix} is the shape a per-sample scoring method returns: a numeric
matrix of \strong{pathways (rows) x samples (columns)}, carrying enough metadata
to be tested or plotted without the caller re-supplying it.
}
\details{
Attributes:
\describe{
\item{\code{database}}{character. The \verb{gsdb_*} provider the sets came from.}
\item{\code{method}}{character, e.g. \code{"gsva"}.}
\item{\code{score_type}}{character. What a cell is, e.g. \code{"gsva"}, \code{"ssgsea"},
\code{"zscore"}, \code{"plage"}. Renderers label the colour bar from this.}
\item{\code{pathway_names}}{named character vector mapping \code{rownames()} to
human-readable labels. Renderers use it for axis text.}
\item{\code{sample_data}}{data frame of sample metadata, one row per column of
the matrix, in column order — this is what \code{gs_test()} builds a design
from.}
}

\code{gs_test()} dispatching on a \code{gs_matrix} is the GSVA -> limma pipeline; it
returns a \link{gs_result} with \code{stat_type = "t"}.
}
