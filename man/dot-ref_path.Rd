% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ref-path.R
\name{.ref_path}
\alias{.ref_path}
\title{Resolve a path in the shared reference cache}
\usage{
.ref_path(source, ..., path = NULL, root = NULL, must_exist = TRUE)
}
\arguments{
\item{source}{Character scalar naming one refcache source. It must be a
single path segment.}

\item{...}{Character scalar path components below the source's \code{current}
snapshot. Ignored when \code{path} is supplied.}

\item{path}{Optional caller-supplied path. Used as given.}

\item{root}{Optional refcache root. \code{NULL} reads \code{REFCACHE_ROOT}.}

\item{must_exist}{Logical scalar; whether the returned path must exist.}
}
\value{
A character scalar with \code{snapshot}, \code{source}, \code{root}, and
\code{caller_supplied} attributes. \code{snapshot} and \code{root} are \code{NA_character_}
for a caller-supplied path.
}
\description{
An explicit \code{path} takes precedence over the shared cache. Otherwise the
path is resolved below \verb{$REFCACHE_ROOT/<source>/current}, and the snapshot
selected by \code{current} is recorded for session provenance.
}
\keyword{internal}
