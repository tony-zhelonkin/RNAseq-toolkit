% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-bfc.R
\name{de_bfc_plot}
\alias{de_bfc_plot}
\title{B-statistic versus log2 fold-change plot}
\usage{
de_bfc_plot(
  de_results,
  fc_cutoff = 2,
  B_cutoff = 0,
  max.overlaps = 10,
  top_n = 5,
  highlight_gene = NULL,
  label_method = "top",
  x_breaks = 2,
  title = "B-statistic vs Log2FC",
  color_palette = c(NS = "#7F7F7F", Log2FC = "#0173B2", `B-statistic` = "#029E73",
    `B-statistic & Log2FC` = "#D55E00"),
  y_padding = 1,
  show_grid = FALSE
)
}
\arguments{
\item{de_results}{Data frame whose rownames are gene IDs, carrying \code{logFC}
and \code{B} (limma \code{topTable()} shape).}

\item{fc_cutoff}{Numeric absolute log2 fold-change threshold.}

\item{B_cutoff}{Numeric B-statistic threshold.}

\item{max.overlaps}{Passed to \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.}

\item{top_n}{Integer genes labelled per side, ranked by decreasing \code{B}.}

\item{highlight_gene}{Character vector of gene IDs always labelled, bold and
black.}

\item{label_method}{One of \code{"top"}, \code{"sig"}, \code{"B"}, \code{"log2fc"}; anything
else labels nothing.}

\item{x_breaks}{Numeric spacing between fold-change axis ticks.}

\item{title}{Plot title.}

\item{color_palette}{Named colours for \code{NS}, \code{Log2FC}, \code{B-statistic} and
\code{B-statistic & Log2FC}.}

\item{y_padding}{Numeric head-room kept around \code{B_cutoff}.}

\item{show_grid}{Logical. Keep the panel grid.}
}
\value{
A \code{ggplot} object.
}
\description{
limma's \code{B} is the log-odds that a gene is differentially expressed, so
\code{B > 0} means "more likely DE than not". Plotting it against fold change
gives an evidence-versus-effect view that does not depend on a p-value
threshold. The y limits always leave room around \code{B_cutoff}, so the
threshold line stays visible even when no gene crosses it — in that case an
italic annotation names the line explicitly.
}
\examples{
de <- data.frame(
  logFC = c(-3, -1, 0.2, 1.5, 4),
  B     = c(4, -1, -3, 0.5, 6),
  row.names = paste0("Gene", 1:5)
)
de_bfc_plot(de, fc_cutoff = 1)
}
