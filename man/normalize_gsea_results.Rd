% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-fixtures-gs.R
\name{normalize_gsea_results}
\alias{normalize_gsea_results}
\title{Deprecated: normalize a GSEA result to a standard tibble}
\usage{
normalize_gsea_results(
  gsea_obj,
  database,
  contrast,
  padj_cutoff = 1,
  format_names = TRUE,
  max_name_length = 80,
  atlas_universe = NULL
)
}
\arguments{
\item{gsea_obj}{A \code{\link[=gs_result]{gs_result()}} or data frame; \code{NULL} returns
\code{empty_gsea_tibble()}.}

\item{database}{Character. Name of the source database.}

\item{contrast}{Character. Name of the contrast.}

\item{padj_cutoff}{Numeric. Filter to \code{padj < padj_cutoff} (default: 1, no
filter).}

\item{format_names}{Logical. Apply \code{\link[=format_pathway_name]{format_pathway_name()}} to
\code{pathway_name} (default: TRUE).}

\item{max_name_length}{Integer. Truncate \code{pathway_name} to this length
(default: 80).}

\item{atlas_universe}{Character vector or \code{NULL}. Accepted and ignored,
with a warning when supplied: a \code{\link[=gs_result]{gs_result()}} carries no gene-set
membership, so there is nothing to intersect against a universe. Use
\code{\link[=gs_leading_edge]{gs_leading_edge()}} with the original \code{gs_db} instead.}
}
\value{
A \code{\link[=gs_result]{gs_result()}}.
}
\description{
Superseded by \code{\link[=gs_result]{gs_result()}} -- which \code{\link[=gs_test]{gs_test()}} (and this file's
\code{run_gsea()} shim) now returns directly, so there is usually nothing left
to normalize. This shim accepts a \code{\link[=gs_result]{gs_result()}} or a plain data frame and
stamps in \code{database}/\code{contrast}, matching the old function's job of
attaching those labels to a per-database result.
}
\keyword{internal}
