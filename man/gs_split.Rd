% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-ops.R
\name{gs_split}
\alias{gs_split}
\title{Split a \code{gs_result} into a named list}
\usage{
gs_split(x, by = c("database", "contrast"), drop_empty = TRUE)
}
\arguments{
\item{x}{A \link{gs_result}.}

\item{by}{Character vector of columns to split on. Default
\code{c("database", "contrast")}.}

\item{drop_empty}{Logical. Drop combinations with no rows.}
}
\value{
A named list of \link{gs_result} objects, named by the \code{by} values joined
with \code{"."}.
}
\description{
The inverse of the \code{rbind()} that pools contrasts.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "Set A", SET_B = "Set B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
gs_split(res, by = "contrast")
}
