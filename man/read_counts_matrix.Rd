% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-io.R
\name{read_counts_matrix}
\alias{read_counts_matrix}
\title{Read a wide counts table into a matrix}
\usage{
read_counts_matrix(path)
}
\arguments{
\item{path}{Path to the counts file (tab, comma or semicolon delimited;
detected from the header).}
}
\value{
A numeric matrix, genes in rows, with an \code{input_gene_name}
attribute.
}
\description{
Accepts the four shapes a bulk RNA-seq counts file actually arrives in:
}
\details{
\itemize{
\item featureCounts, post-processed -- \code{Geneid} plus sample columns.
\item featureCounts, raw -- \verb{Geneid, Chr, Start, End, Strand, Length} then
samples from column 7 on.
\item Salmon gene level -- \code{gene_id}, \code{gene_name}, then samples.
\item generic -- \code{gene_id} plus sample columns.
}

Sample column names are stripped of directory paths and of the
\code{.bam}/\code{.sam}/\code{.sorted}/\code{.markdup}/\code{.txt} suffixes aligners leave behind.

For the Salmon shape the original \code{gene_name} column is not discarded: it is
returned as an \code{input_gene_name} attribute, a character vector named by
\strong{version-stripped} stable ID, so \code{\link[=annotate_genes]{annotate_genes()}} can fall back to the
quantifier's own symbol when Ensembl has none. The attribute is \code{NA} for the
other shapes.
}
\examples{
f <- tempfile(fileext = ".tsv")
write.table(data.frame(Geneid = c("ENSG1", "ENSG2"), S1 = c(3, 4),
                       S2 = c(5, 6)),
            f, sep = "\t", row.names = FALSE, quote = FALSE)
read_counts_matrix(f)
}
