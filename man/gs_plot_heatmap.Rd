% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-heatmap.R
\name{gs_plot_heatmap}
\alias{gs_plot_heatmap}
\title{Gene-set heatmap}
\usage{
gs_plot_heatmap(x, ...)
}
\arguments{
\item{x}{A \link{gs_result} (tiles are the signed statistic) or a \link{gs_matrix}
(tiles are the per-sample scores).}

\item{...}{Passed to the method.}
}
\value{
A ggplot object, carrying its source table in the \code{gs_source}
attribute.
}
\description{
Pathways down the y axis, and along x either the contrasts/databases of a
\link{gs_result} or the samples of a \link{gs_matrix}. One generic covers what used to
be six heatmap functions built on \pkg{pheatmap}; the return value is a
ggplot like every other renderer, so \code{\link[=gs_save]{gs_save()}} handles it.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "SET_A", SET_B = "SET_B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
gs_plot_heatmap(res)
}
