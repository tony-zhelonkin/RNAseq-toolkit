% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsdb-coresh.R
\name{gsdb_coresh}
\alias{gsdb_coresh}
\title{Build a CoReSh-derived gene-set database}
\usage{
gsdb_coresh(
  queries,
  chunk_dir = NULL,
  species = "human",
  top_hits = 5L,
  top_n = 50L,
  min_size = 15L,
  max_size = 500L,
  jaccard_threshold = 0.8,
  n_cores = 4L,
  seed = 1L
)
}
\arguments{
\item{queries}{A non-empty named list of integer Entrez vectors, each with
at least three unique IDs.}

\item{chunk_dir}{Optional explicit path to an \code{hsa} or \code{mmu} chunk directory.
When \code{NULL}, resolve the corresponding directory through the shared
refcache.}

\item{species}{A human or mouse alias accepted by \code{\link[=.species]{.species()}}.}

\item{top_hits}{Positive whole number of top CoReSh datasets to retain per
query.}

\item{top_n}{Positive whole number of absolute loadings to retain per hit.}

\item{min_size}{Positive whole-number minimum set size.}

\item{max_size}{Positive whole-number maximum set size.}

\item{jaccard_threshold}{Numeric threshold in \verb{[0, 1]} passed to
\code{\link[=coresh_sets]{coresh_sets()}}.}

\item{n_cores}{Positive whole number passed to \code{\link[=coresh_search]{coresh_search()}}.}

\item{seed}{Whole-number RNG seed pinned around the compendium search. The
default matches the upstream CoReSh reference implementation.}
}
\value{
A \code{\link[=gs_db]{gs_db()}} with \code{database = "coresh"}. Its database provenance
records the resolved snapshot, species, query names and unique Entrez-ID
counts, and every set-building parameter. It does not store the exact
Entrez IDs; reproducing the database requires the caller's own query
definitions. Set provenance identifies the query, dataset, chunk, loading
cutoff, and CoReSh rank for each retained set.
}
\description{
Searches the CoReSh compendium by percentage of variance explained, keeps
the requested number of datasets per query, and turns their strongest gene
loadings into a \code{\link[=gs_db]{gs_db()}}. This is a provider-layer composition of
\code{\link[=coresh_search]{coresh_search()}} and \code{\link[=coresh_sets]{coresh_sets()}}; it does not score, extract, map, or
de-duplicate genes itself.
}
\examples{
\dontrun{
db <- gsdb_coresh(
  list(iron_uptake = c(7037L, 4891L, 55240L)),
  species = "human",
  n_cores = 1L
)
}
}
