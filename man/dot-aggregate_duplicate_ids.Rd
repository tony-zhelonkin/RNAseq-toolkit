% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-io.R
\name{.aggregate_duplicate_ids}
\alias{.aggregate_duplicate_ids}
\title{Collapse duplicate row IDs by summing}
\usage{
.aggregate_duplicate_ids(mat, ids = rownames(mat))
}
\arguments{
\item{mat}{Numeric matrix.}

\item{ids}{Character vector of length \code{nrow(mat)}; defaults to
\code{rownames(mat)}.}
}
\value{
A numeric matrix with unique rownames.
}
\description{
Rows sharing an ID are collapsed with \code{colSums}. Output rownames come out in
\code{split()} order — \code{sort(unique(ids))}, \strong{not} input order — so callers that
carry a parallel annotation table must re-match to \code{rownames()} afterwards.
}
\details{
The \code{input_gene_name} attribute survives: for each group the first non-\code{NA}
gene name is kept and re-ordered to the collapsed row order.
}
\keyword{internal}
