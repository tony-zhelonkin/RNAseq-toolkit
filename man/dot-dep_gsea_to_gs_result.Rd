% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated-plot.R
\name{.dep_gsea_to_gs_result}
\alias{.dep_gsea_to_gs_result}
\title{Convert a bare \code{gseaResult} to a minimal \code{gs_result} for the plot shims}
\usage{
.dep_gsea_to_gs_result(gsea_obj)
}
\arguments{
\item{gsea_obj}{A \code{gseaResult} object.}
}
\value{
A \code{gs_result}, with \code{attr(., "ranks")} set to \code{gsea_obj@geneList}
and \code{attr(., "gene_sets")} set to \code{gsea_obj@geneSets}, for
\code{gs_plot_running()}'s fallback.
}
\description{
The frozen plot names (\code{gsea_dotplot()}, \code{gsea_dotplot_facet()},
\code{gsea_barplot()}, \code{gsea_running_sum_plot()}) only ever took a bare
\code{gseaResult} -- no \code{database}/\code{contrast} metadata -- so they cannot go
through \code{normalize_gsea_results()}, whose old formals require both with no
default. This builds just enough of a \code{gs_result} to drive the new
renderers: \code{pathway_id}/\code{pathway_name} from \code{ID}/\code{Description}, \code{stat}/
\code{stat_type} from \code{NES}/\code{"NES"}, \code{p_value}/\code{padj} from \code{pvalue}/\code{p.adjust},
\code{n_genes}/\code{n_genes_tested} both from \code{setSize} (the old object carries no
better proxy for genes-tested), and \code{leading_edge} from splitting
\code{core_enrichment} on \code{"/"}. \code{database} and \code{contrast} are filled with a
constant placeholder since the frozen callers never supplied either.
}
\keyword{internal}
