% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-fixtures-plot.R
\name{.dep_gsea_to_gs_result}
\alias{.dep_gsea_to_gs_result}
\title{Convert a bare \code{gseaResult} (or pass through a \code{gs_result}) for the plot
shims}
\usage{
.dep_gsea_to_gs_result(gsea_obj)
}
\arguments{
\item{gsea_obj}{A \code{gseaResult} object, or a \code{gs_result} (e.g. from the
\code{run_gsea()} shim).}
}
\value{
A \code{gs_result}. When \code{gsea_obj} is already a \code{gs_result} it is
returned unchanged (attributes and all). When it is a \code{gseaResult},
\code{attr(., "ranks")} is set to \code{gsea_obj@geneList} and
\code{attr(., "gene_sets")} to \code{gsea_obj@geneSets}, for \code{gs_plot_running()}'s
fallback.
}
\description{
The frozen plot names (\code{gsea_dotplot()}, \code{gsea_dotplot_facet()},
\code{gsea_barplot()}, \code{gsea_running_sum_plot()}) historically only ever took a
bare \code{gseaResult} -- no \code{database}/\code{contrast} metadata -- so they cannot go
through \code{normalize_gsea_results()}, whose old formals require both with no
default. For that case, this builds just enough of a \code{gs_result} to drive
the new renderers: \code{pathway_id}/\code{pathway_name} from \code{ID}/\code{Description},
\code{stat}/\code{stat_type} from \code{NES}/\code{"NES"}, \code{p_value}/\code{padj} from
\code{pvalue}/\code{p.adjust}, \code{n_genes}/\code{n_genes_tested} both from \code{setSize} (the old
object carries no better proxy for genes-tested), and \code{leading_edge} from
splitting \code{core_enrichment} on \code{"/"}. \code{database} and \code{contrast} are filled
with a constant placeholder since the frozen callers never supplied either.
}
\details{
But the single most common legacy call pattern is \code{run_gsea()} piped
straight into one of these four -- and \code{run_gsea()} is itself a deprecated
shim that now returns a \code{gs_result}, not an S4 \code{gseaResult}. That object
already carries \code{attr(x, "ranks")}/\code{attr(x, "gene_sets")} when it came from
\code{run_gsea()}, so it is passed through untouched rather than re-derived.
}
\keyword{internal}
