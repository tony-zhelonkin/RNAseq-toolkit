% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-utils.R
\name{.de_theme}
\alias{.de_theme}
\title{Theme used by the \verb{de_*} renderers}
\usage{
.de_theme(base_size = 12)
}
\arguments{
\item{base_size}{Base font size.}
}
\value{
A \code{ggplot2} theme object.
}
\description{
Resolves to the shared \code{theme_bulki()} look when that renderer-layer theme is
present in the package namespace, and otherwise falls back to a
self-contained minimal theme with the same visual contract (white
background, visible axis lines, no grid). The indirection exists because the
DE module and the theme live in different modules of the refactor.
}
\details{
Two details are load-bearing, both of which this function got wrong when the
modules were first merged:
\itemize{
\item \code{base_size} is \strong{forwarded}. The original called \code{theme_bulki()} with no
arguments, so a caller asking for \code{base_size = 20} silently got 14.
\item The floor is \strong{skipped} (\code{floor = NULL}). The DE renderers' documented
default is 12 pt and their geometry derives from it, so \code{theme_bulki()}'s
deliberate 14 pt floor scaled every volcano and MD plot by 14/12 -- point
stroke 1.5 -> 1.75, line widths to match. The floor still applies to the
\verb{gs_plot_*} layer it was designed for.
}
}
\keyword{internal}
