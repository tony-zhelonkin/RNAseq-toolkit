% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-db.R
\name{gs_db}
\alias{gs_db}
\title{The \code{gs_db} gene-set database object}
\usage{
gs_db(
  sets,
  database,
  species,
  pathway_names = NULL,
  database_label = NULL,
  gene_id_type = "symbol",
  pathway_descriptions = NULL
)
}
\arguments{
\item{sets}{Named list of character vectors: set id -> gene symbols.}

\item{database}{Character(1) registry key, e.g. \code{"mito_unified"}.}

\item{species}{Character(1) species, e.g. \code{"Mus musculus"}.}

\item{pathway_names}{Named character of human-readable labels, or \code{NULL}
to derive them from the ids.}

\item{database_label}{Character(1) display name, or \code{NULL} to reuse
\code{database}.}

\item{gene_id_type}{Character(1); only \code{"symbol"} is supported.}

\item{pathway_descriptions}{Named character of longer descriptions, or
\code{NULL} when the provider has none. Not required to cover every id.}
}
\value{
A \code{gs_db} object.
}
\description{
Every \verb{gsdb_*} provider returns a \code{gs_db}: a named list of character
vectors mapping set id to gene symbols, which is exactly the shape
\code{\link[fgsea:fgseaMultilevel]{fgsea::fgseaMultilevel()}}, \code{\link[fgsea:fora]{fgsea::fora()}} and \code{GSVA::gsvaParam()} take.
}
\details{
Attributes carried alongside the list:
\itemize{
\item \code{pathway_names} -- named character, id -> human-readable label, with
\code{names()} identical to \code{names(sets)}.
\item \code{database} -- the \strong{machine-typeable registry key} (\code{"mitopathways"},
\code{"msigdb_H"}); lands in \code{gs_result$database}, where it is a join and
filter key, so it must be stable rather than pretty.
\item \code{database_label} -- the display string (\code{"MitoPathways 3.0"}). Renderers
prettify from here; the data layer never depends on it.
\item \code{species} -- \code{"Mus musculus"} or \code{"Homo sapiens"}.
\item \code{gene_id_type} -- \code{"symbol"} (the only value today).
\item \code{pathway_descriptions} -- optional named character, id -> full-sentence
description (e.g. MSigDB's \code{gs_description}), or \code{NULL} when a provider
has none. Unlike \code{pathway_names} this is not guaranteed to cover every
id. Carried through \verb{[.gs_db} like every other attribute.
}

Empty sets are dropped at construction, genes are de-duplicated within a
set, and set ids must be unique.
}
\keyword{internal}
