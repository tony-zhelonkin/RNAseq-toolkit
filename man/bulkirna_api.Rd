% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{bulkirna_api}
\alias{bulkirna_api}
\title{Public API stability registry}
\usage{
bulkirna_api(lifecycle = "all", quiet = FALSE)
}
\arguments{
\item{lifecycle}{\code{"all"}, or one or more of \code{"stable"}, \code{"experimental"},
and \code{"deprecated"}.}

\item{quiet}{Logical. If \code{FALSE}, print the registry and return it
invisibly. If \code{TRUE}, do not print and return it visibly.}
}
\value{
A tibble with one row per selected exported function and columns \code{name},
\code{layer}, \code{lifecycle}, \code{frozen}, \code{superseded_by}, and \code{removed_in}.
}
\description{
\code{bulkirna_api()} is the machine-readable contract for the package's public
surface. Stability and the historical signature freeze are separate axes:
}
\details{
\itemize{
\item \code{stable} functions follow semantic versioning. Incompatible changes need
a major release, apart from removals that completed a documented
deprecation cycle.
\item \code{experimental} functions may change or be removed without a major release.
\item \code{deprecated} functions remain callable and warn, but are scheduled for
removal in the version recorded in \code{removed_in}.
}

\code{frozen} records the 24 signatures carried forward from the script-library
API. It does not mean that a function is recommended: all 20 deprecated
shims are frozen until their documented removal. Conversely, most stable
functions were introduced after the freeze and therefore have
\code{frozen = FALSE}.

The \code{superseded_by} field may name a technique or a sequence of calls rather
than a single function, and may say that no replacement exists when part of
the deprecated behaviour was deliberately retired.
\code{layer} records the function's functional module. Stable and experimental
functions retain their curated registry group; deprecated compatibility
shims, which have no curated group, derive it from their name prefix.
}
\examples{
experimental <- bulkirna_api("experimental", quiet = TRUE)
}
