% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-test.R
\name{.gs_limma}
\alias{.gs_limma}
\title{limma-over-scores adapter}
\usage{
.gs_limma(
  x,
  design = NULL,
  contrast = "contrast",
  coef = NULL,
  block = NULL,
  trend = FALSE,
  robust = FALSE
)
}
\arguments{
\item{x}{A \code{gs_matrix}.}

\item{design}{A model matrix with one row per sample, or a one-sided formula
evaluated in the matrix's \code{sample_data}.}

\item{contrast}{A contrast specification: a string such as \code{"KO-WT"} passed
to \code{\link[limma:makeContrasts]{limma::makeContrasts()}}, a numeric contrast vector, or a plain label
when \code{coef} is used instead.}

\item{coef}{Coefficient of \code{design} to test when \code{contrast} is only a label.
Defaults to the last column.}

\item{block}{Optional blocking factor for \code{\link[limma:duplicateCorrelation]{limma::duplicateCorrelation()}}.}

\item{trend, robust}{Passed to \code{\link[limma:eBayes]{limma::eBayes()}}.}
}
\value{
A data frame of core \link{gs_result} columns.
}
\description{
The GSVA half of the GSVA -> limma pipeline, run on a \link{gs_matrix} as if its
pathway scores were genes.
}
\keyword{internal}
