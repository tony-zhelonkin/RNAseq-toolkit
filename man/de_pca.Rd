% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-pca.R
\name{de_pca}
\alias{de_pca}
\title{Two-dimensional PCA of a DGEList}
\usage{
de_pca(
  dge,
  colour_by = "group",
  shape_by = NULL,
  label = TRUE,
  title = "PCA Plot",
  xlim_abs = NULL,
  ylim_abs = NULL,
  point_size = 5,
  label_size = 4,
  palette = c("#E69F00", "#56B4E9", "#009E73", "#F0E442", "#0072B2", "#D55E00",
    "#CC79A7", "#000000")
)
}
\arguments{
\item{dge}{A \code{DGEList}, e.g. from \code{\link[=build_dge]{build_dge()}}.}

\item{colour_by}{Name of a \code{dge$samples} column mapped to point colour, or
\code{NULL} for one colour.}

\item{shape_by}{Name of a \code{dge$samples} column mapped to point shape, or
\code{NULL}.}

\item{label}{Logical. Draw the \code{colour_by} value above each point.}

\item{title}{Plot title.}

\item{xlim_abs}{Optional symmetric x-axis limits, applied as a zoom
(\code{coord_fixed()}), so a sample outside them is clipped from view rather
than dropped from the figure. \code{NULL} derives them from the data with 10\%
head-room.}

\item{ylim_abs}{Optional symmetric y-axis limits, with the same zoom
behaviour as \code{xlim_abs}.}

\item{point_size}{Numeric point size.}

\item{label_size}{Numeric label size.}

\item{palette}{Character vector of colours recycled across the
\code{colour_by} levels.}
}
\value{
A \code{ggplot} object.
}
\description{
PCA on logCPM, plotted on a fixed 1:1 aspect ratio so distances read
correctly. Grouping is by metadata column name -- nothing about organs,
tissues or study design is hard-coded.
}
\examples{
if (requireNamespace("edgeR", quietly = TRUE)) {
  set.seed(1)
  counts <- matrix(rpois(400, 50), nrow = 100,
    dimnames = list(paste0("G", 1:100), paste0("S", 1:4)))
  samples <- data.frame(group = c("WT", "WT", "KO", "KO"),
                        row.names = colnames(counts))
  dge <- build_dge(counts, samples, data.frame(gene = rownames(counts)))
  de_pca(dge, colour_by = "group")
}
}
