% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-fixtures-plot.R
\name{plot_all_gsea_results}
\alias{plot_all_gsea_results}
\title{Plot all GSEA results for a given analysis (deprecated)}
\usage{
plot_all_gsea_results(
  gsea_list,
  analysis_name,
  out_root,
  n_pathways = 20,
  padj_cutoff = 0.05,
  expr_data = NULL,
  sample_annotation = NULL,
  sample_order = NULL,
  ann_colors = NULL
)
}
\arguments{
\item{gsea_list}{List of GSEA results: \code{gseaResult} objects, \code{gs_result}
objects (e.g. from the \code{run_gsea()} shim), or a mix of both.}

\item{analysis_name}{Name of the analysis.}

\item{out_root}{Output root directory.}

\item{n_pathways}{Number of pathways to show.}

\item{padj_cutoff}{Adjusted p-value cutoff.}

\item{expr_data}{Expression data matrix for heatmaps. Not supported by
\code{.gs_plot_all()}; accepted and ignored (see \verb{@note}).}

\item{sample_annotation}{Sample annotation data.frame. Not supported by
\code{.gs_plot_all()}; accepted and ignored (see \verb{@note}).}

\item{sample_order}{Order of samples for plots. Not supported by
\code{.gs_plot_all()}; accepted and ignored (see \verb{@note}).}

\item{ann_colors}{Colours for annotations. Not supported by
\code{.gs_plot_all()}; accepted and ignored (see \verb{@note}).}
}
\value{
A character vector of every written path, invisibly, as returned
by \code{.gs_plot_all()}.
}
\description{
Deprecated: the new golden path is to call the \code{\link[=gs_plot_dot]{gs_plot_dot()}} /
\code{\link[=gs_plot_bar]{gs_plot_bar()}} renderer you want and save it with \code{\link[=gs_save]{gs_save()}}. This shim
reproduces the old formals of \code{plot_all_gsea_results()} verbatim and
forwards to the internal \code{.gs_plot_all()}, which holds the ported body.
}
\note{
\code{gsea_list} (a named list of \code{gseaResult}/data-frame objects keyed by
database) is converted to a single \code{gs_result} via the internal
\code{.dep_gsea_to_gs_result()}, with each element's list name substituted in
as its \code{database} column so \code{.gs_plot_all()}'s per-database loop still
works.

\code{expr_data}, \code{sample_annotation}, \code{sample_order} and \code{ann_colors}
drove heatmap panels in the old function; \code{.gs_plot_all()} (the ported
body behind this shim) does not render a heatmap, so these four formals
are accepted and ignored. No heatmap is written where the old function
would have written one.
}
\keyword{internal}
