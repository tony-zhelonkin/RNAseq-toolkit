% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coresh.R
\name{coresh_search}
\alias{coresh_search}
\title{Search the CoReSh compendium}
\usage{
coresh_search(
  queries,
  chunk_dir = NULL,
  species = "human",
  n_cores = 4L,
  pvalues = FALSE
)
}
\arguments{
\item{queries}{A non-empty named list of integer Entrez vectors, each of
length at least three.}

\item{chunk_dir}{Optional explicit path to an \code{hsa} or \code{mmu} chunk directory.}

\item{species}{One of \code{"human"}, \code{"hsa"}, \code{"mouse"}, or \code{"mmu"}.}

\item{n_cores}{Positive whole number. \code{1} uses base R and does not require
BiocParallel.}

\item{pvalues}{Logical. Must currently be \code{FALSE}.}
}
\value{
A tibble with columns \code{query_name}, \code{gse}, \code{gpl}, \code{pct_var},
\code{p_value}, \code{size}, and \code{rank}, ordered by descending \code{pct_var} within
query. Its \code{provenance} attribute records the reference snapshot.
}
\description{
Scores each named Entrez query against every dataset for one species. With
more than one core, work is parallelized over chunk files so each worker
holds only one chunk at a time.
}
\examples{
\dontrun{
hits <- coresh_search(
  list(iron = c(7037L, 4891L, 55240L)),
  species = "human",
  n_cores = 1L
)
}
}
