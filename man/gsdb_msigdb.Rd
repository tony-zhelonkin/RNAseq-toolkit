% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsdb-msigdb.R
\name{gsdb_msigdb}
\alias{gsdb_msigdb}
\title{Load an MSigDB collection}
\usage{
gsdb_msigdb(
  species = "Mus musculus",
  collection = "H",
  subcollection = NULL,
  db_species = c("HS", "MM"),
  min_size = NULL,
  max_size = NULL,
  verbose = FALSE
)
}
\arguments{
\item{species}{Character(1) target species, e.g. \code{"Mus musculus"} or
\code{"Homo sapiens"}. Human sets are mapped to orthologs when \code{db_species}
is \code{"HS"}.}

\item{collection}{Character(1) MSigDB collection, e.g. \code{"H"} for the
hallmarks or \code{"C2"}. See \code{\link[msigdbr:msigdbr_collections]{msigdbr::msigdbr_collections()}}.}

\item{subcollection}{Character(1) sub-collection such as \code{"CP:REACTOME"},
or \code{NULL} for the whole collection.}

\item{db_species}{Character(1) source database, \code{"HS"} (human MSigDB, the
default, with ortholog mapping) or \code{"MM"} (mouse-native sets).}

\item{min_size, max_size}{Integer(1) or \code{NULL}; drop sets outside these
bounds.}

\item{verbose}{Logical(1); message what was loaded.}
}
\value{
A \code{\link[=gs_db]{gs_db()}} whose \code{database} label is the collection, e.g.
\code{"MSigDB H"}.
}
\description{
Thin provider over \code{\link[msigdbr:msigdbr]{msigdbr::msigdbr()}}. MSigDB is downloaded at first use
and cached under \verb{$HOME/.cache/R/msigdbr}, so a cold cache needs network
access.
}
\examples{
\dontshow{if (requireNamespace("msigdbr", quietly = TRUE) && interactive()) withAutoprint(\{ # examplesIf}
db <- gsdb_msigdb("Mus musculus", collection = "H")
summary(db)[1:3, ]
\dontshow{\}) # examplesIf}
}
