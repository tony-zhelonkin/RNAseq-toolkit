% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-theme.R
\name{theme_bulki}
\alias{theme_bulki}
\title{The bulkiRNA plot theme}
\usage{
theme_bulki(base_size = 14, base_family = "", grid = FALSE)
}
\arguments{
\item{base_size}{Base font size in points. Values below \code{14} are raised to
\code{14}.}

\item{base_family}{Base font family. Default \code{""} (the device default).}

\item{grid}{Logical. \code{FALSE} (default) removes all grid lines; \code{TRUE} keeps
a light major grid on both axes.}
}
\value{
A ggplot2 theme object.
}
\description{
A clean, publication-oriented theme: white background, visible axis lines
and ticks, no panel border, and -- by default -- no grid. Every \verb{gs_plot_*}
and \verb{de_*} renderer ends with it, so figure styling is one function rather
than a \code{theme()} call an agent might forget.
}
\details{
The \strong{14 pt base-size floor} lives here on purpose. A figure destined for a
figure panel is unreadable below it, so \code{base_size} values under 14 are
raised to 14 rather than honoured. Pass \code{base_size = 14} explicitly if you
want the floor to be visible in your code.
}
\examples{
ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point() +
  theme_bulki()
}
