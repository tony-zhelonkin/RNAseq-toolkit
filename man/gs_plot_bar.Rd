% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-bar.R
\name{gs_plot_bar}
\alias{gs_plot_bar}
\title{Gene-set barplot}
\usage{
gs_plot_bar(
  x,
  top = 20,
  sort_by = c("stat", "padj", "p_value", "stat_signed"),
  direction = c("both", "up", "down"),
  facet = c("none", "direction", "database", "contrast"),
  padj_max = NULL,
  highlight = 0.05,
  limits = NULL,
  colours = .gs_diverging_colours(),
  wrap_width = 50,
  strip_prefix = TRUE,
  title = NULL,
  database_labels = NULL
)
}
\arguments{
\item{x}{A \link{gs_result}.}

\item{top}{Number of pathways to display, per selection group.}

\item{sort_by}{Selection metric: \code{"stat"} (default, largest absolute value),
\code{"padj"}, \code{"p_value"} or \code{"stat_signed"}.}

\item{direction}{Restrict to \code{"both"} (default), \code{"up"} or \code{"down"}.}

\item{facet}{Row facets: \code{"none"} (default), \code{"direction"}, \code{"database"} or
\code{"contrast"}.}

\item{padj_max}{Optional hard FDR filter applied before selection.}

\item{highlight}{FDR threshold for the black bar outline, or \code{NULL}.}

\item{limits}{Length-2 numeric fill limits. \code{NULL} (default) derives
symmetric limits \strong{from this figure's own data}, so the same statistic can
take a different colour in two figures; pass an explicit \code{limits} when
panels are meant to be compared. The old renderer used a fixed
\code{c(-3.5, 3.5)}. Values outside are squished, not dropped.}

\item{colours}{Length-3 character vector -- low, mid, high fill colours.}

\item{wrap_width}{Soft character width for wrapping pathway labels.}

\item{strip_prefix}{Logical, passed to \code{\link[=format_pathway_name]{format_pathway_name()}}.}

\item{title}{Plot title, or \code{NULL}.}

\item{database_labels}{Named character vector mapping \code{database} keys to
display labels.}
}
\value{
A ggplot object, carrying its source table in the \code{gs_source}
attribute.
}
\description{
Horizontal bars of the signed statistic, one per pathway, filled by the same
diverging scale the dotplot uses. The axis label comes from the object's
\code{stat_type}, so the bars are labelled \code{NES}, \verb{t statistic} or
\verb{log2 fold enrichment} as appropriate.
}
\details{
Unlike the old \code{gsea_barplot()}, non-significant pathways are \strong{not silently
dropped}: \code{top} and \code{sort_by} choose what is shown and \code{highlight} outlines
the significant ones. Pass \code{padj_max} if you do want a hard significance
filter.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "SET_A", SET_B = "SET_B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
gs_plot_bar(res, top = 5)
}
