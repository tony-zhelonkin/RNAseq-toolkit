% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-test.R
\name{.gs_fgsea}
\alias{.gs_fgsea}
\title{fgsea adapter}
\usage{
.gs_fgsea(
  ranks,
  sets,
  min_size = 10L,
  max_size = 500L,
  eps = 0,
  n_perm_simple = 100000L,
  score_type = c("std", "pos", "neg"),
  seed = 123L
)
}
\arguments{
\item{ranks}{Named numeric vector, decreasing.}

\item{sets}{Named list of character vectors.}

\item{min_size, max_size}{Integer set-size bounds, applied after intersecting
with the data. Defaults \code{10} and \code{500} -- the values
\code{clusterProfiler::GSEA()} forwarded to fgsea, kept so results are
unchanged.}

\item{eps}{Numeric. fgsea's p-value accuracy floor; \code{0} means "no floor",
the setting the legacy pipeline used.}

\item{n_perm_simple}{Integer, fgsea's \code{nPermSimple}.}

\item{score_type}{One of \code{"std"}, \code{"pos"}, \code{"neg"}.}

\item{seed}{Integer seed set immediately before the fgsea call, or \code{NULL}
to leave the RNG untouched. When set, the generator is pinned as well.}
}
\value{
A data frame of core \link{gs_result} columns.
}
\description{
fgsea adapter
}
\keyword{internal}
