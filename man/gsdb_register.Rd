% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsdb-file.R
\name{gsdb_register}
\alias{gsdb_register}
\title{Register a user-supplied list of gene sets as a database}
\usage{
gsdb_register(
  sets,
  database,
  species = "Mus musculus",
  pathway_names = NULL,
  min_size = NULL,
  max_size = NULL
)
}
\arguments{
\item{sets}{Named list of character vectors: set id -> gene symbols.}

\item{database}{Character(1) provider label; lands in
\code{gs_result$database}.}

\item{species}{Character(1) species the symbols belong to.}

\item{pathway_names}{Named character of human-readable labels, or \code{NULL}
to use the ids.}

\item{min_size, max_size}{Integer(1) or \code{NULL}; drop sets outside these
bounds.}
}
\value{
A \code{gs_db}.
}
\description{
Turns a plain named list of gene symbols into a first-class \code{\link[=gs_db]{gs_db()}}, so
ad-hoc signatures go through \code{gs_test()} / \code{gs_score()} exactly like the
bundled databases.
}
\examples{
gsdb_register(
  list(MY_SET = c("Actb", "Gapdh"), OTHER = c("Sdha", "Ndufa1")),
  database = "my signatures",
  species = "Mus musculus"
)
}
