% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-volcano.R
\name{de_volcano}
\alias{de_volcano}
\title{Volcano plot for differential-expression results}
\usage{
de_volcano(
  de_results,
  decision_by = c("fdr", "p"),
  p_cutoff = 0.05,
  fc_cutoff = 2,
  top_n = 5,
  highlight_gene = NULL,
  label_method = "top",
  x_breaks = 1,
  title = "Volcano plot",
  subtitle = NULL,
  caption = NULL,
  fixed_p_boundary = NULL,
  color_palette = c(NS = "#7F7F7F", Log2FC = "#0173B2", `p-value` = "#029E73",
    `p-value & Log2FC` = "#D55E00"),
  show_grid = FALSE,
  max.overlaps = 10,
  annotate_counts = FALSE,
  ...,
  orientation = c("horizontal", "vertical")
)
}
\arguments{
\item{de_results}{Data frame whose \strong{rownames are gene IDs}, carrying at
least \code{logFC}, \code{P.Value} and \code{adj.P.Val} (limma \code{topTable()} shape).}

\item{decision_by}{\code{"fdr"} (default) or \code{"p"}; which column makes the
significance call and therefore the point colour.}

\item{p_cutoff}{Numeric. An FDR threshold when \code{decision_by = "fdr"}, a raw
\emph{p} threshold when \code{decision_by = "p"}.}

\item{fc_cutoff}{Numeric. Absolute log2 fold-change threshold.}

\item{top_n}{Integer. Genes labelled per side, ranked by the deciding
statistic.}

\item{highlight_gene}{Character vector of gene IDs always labelled, in bold
black, with overlap suppression disabled.}

\item{label_method}{One of \code{"top"}, \code{"sig"}, \code{"p"}, \code{"log2fc"}; anything else
labels nothing.}

\item{x_breaks}{Numeric spacing between fold-change axis ticks.}

\item{title}{Plot title.}

\item{subtitle}{Optional subtitle.}

\item{caption}{Optional caption. \code{NULL} generates one documenting the
thresholds actually drawn.}

\item{fixed_p_boundary}{Numeric raw \emph{p} to pin the FDR boundary line to, or
\code{NULL} to derive it from the significant set.}

\item{color_palette}{Named character vector of four colours for \code{NS},
\code{Log2FC}, \code{p-value} and \code{p-value & Log2FC}.}

\item{show_grid}{Logical. Keep the panel grid.}

\item{max.overlaps}{Passed to \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.}

\item{annotate_counts}{Logical. Append a second legend line of up/down
counts under the highest-priority populated significance category.}

\item{...}{Absorbs deprecated arguments such as \code{use_fdr}.}

\item{orientation}{\code{"horizontal"} (default; fold change on \emph{x}) or
\code{"vertical"} (fold change on \emph{y}). Vertical panels stack into a row well --
see \code{\link[=de_volcano_grid]{de_volcano_grid()}}. Must be supplied by name.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots \code{-log10(P.Value)} against \code{logFC} while \strong{deciding significance with
\code{adj.P.Val}}. Raw \emph{p} is a per-gene test statistic and gives maximal
resolution; FDR is an average over the rejected set and produces stair-step
artefacts when many \emph{p} values collapse onto one adjusted value. Keeping raw
\emph{p} on the axis and FDR for the decision buys the multiple-testing guarantee
without the staircase.
}
\details{
The dashed horizontal line is therefore \strong{not} at \code{-log10(p_cutoff)} in FDR
mode. It sits at the raw \emph{p} value that realises the FDR boundary -- the
largest raw \emph{p} among the genes that pass \code{adj.P.Val <= p_cutoff} -- so the
line always coincides with the colour boundary. When no gene passes, no line
is drawn and an italic annotation says so instead.\tabular{lll}{
   \code{decision_by} \tab Colour decision uses \tab Dashed line at \cr
   \code{"fdr"} (default) \tab \code{adj.P.Val <= p_cutoff} \tab \code{-log10} of the largest raw \emph{p} with \code{adj.P.Val <= p_cutoff} \cr
   \code{"p"} \tab \code{P.Value <= p_cutoff} \tab \code{-log10(p_cutoff)} \cr
}


\code{fixed_p_boundary} pins the line to a known raw \emph{p} instead of deriving it.
Use it when \code{de_results} has already been filtered, so the largest surviving
raw \emph{p} no longer marks the true FDR boundary.
}
\examples{
de <- data.frame(
  logFC     = c(-3, -1, 0.2, 1.5, 4),
  P.Value   = c(1e-6, 0.02, 0.7, 0.01, 1e-8),
  adj.P.Val = c(1e-4, 0.06, 0.9, 0.04, 1e-6),
  row.names = paste0("Gene", 1:5)
)
de_volcano(de, p_cutoff = 0.05, fc_cutoff = 1)
}
