% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-dot.R
\name{gs_plot_dot}
\alias{gs_plot_dot}
\title{Gene-set dotplot}
\usage{
gs_plot_dot(
  x,
  top_n = 10,
  aes_x = c("stat", "gene_ratio"),
  sort_by = c("padj", "p_value", "stat", "stat_signed"),
  direction = c("both", "up", "down"),
  facet = c("none", "direction", "database", "contrast"),
  compare = NULL,
  highlight = 0.05,
  label = FALSE,
  size_range = c(2, 10),
  limits = NULL,
  palette = .gs_diverging_colours(),
  wrap_width = 50,
  strip_prefix = TRUE,
  title = NULL,
  database_labels = NULL
)
}
\arguments{
\item{x}{A \link{gs_result}.}

\item{top_n}{Number of pathways to display, per selection group. \code{NULL} shows
all.}

\item{aes_x}{What the x axis carries: \code{"stat"} (default) or \code{"gene_ratio"}
(leading-edge size over set size; requires a \code{leading_edge} or \code{overlap}
column).}

\item{sort_by}{Selection metric: \code{"padj"} (default), \code{"p_value"}, \code{"stat"}
(largest absolute value) or \code{"stat_signed"} (most positive).}

\item{direction}{Restrict to \code{"both"} (default), \code{"up"} or \code{"down"}.}

\item{facet}{Row facets: \code{"none"} (default), \code{"direction"} -- the old
\code{gsea_dotplot_facet()} -- \code{"database"} or \code{"contrast"}. \code{top_n} applies
within each facet.}

\item{compare}{Column facets for side-by-side comparison: \code{NULL} (default),
\code{"contrast"} or \code{"database"}. Pathways are selected once over the whole
object and every panel then shows the same set, so the grid is not ragged.}

\item{highlight}{FDR threshold for the black significance outline, or \code{NULL}
for no outline.}

\item{label}{Logical. Annotate each dot with its FDR, placed with
\pkg{ggrepel}.}

\item{size_range}{Length-2 numeric, the dot size range.}

\item{limits}{Length-2 numeric fill limits. \code{NULL} (default) derives
symmetric limits \strong{from this figure's own data}, so the same statistic can
take a different colour in two figures; pass an explicit \code{limits} when
panels are meant to be compared. The old renderer used a fixed
\code{c(-3.5, 3.5)}. Values outside are squished, not dropped.}

\item{palette}{Length-3 character vector -- low, mid, high fill colours.}

\item{wrap_width}{Soft character width for wrapping pathway labels.}

\item{strip_prefix}{Logical, passed to \code{\link[=format_pathway_name]{format_pathway_name()}}.}

\item{title}{Plot title, or \code{NULL}.}

\item{database_labels}{Named character vector mapping \code{database} keys to
display labels. Only needed when the object carries neither a
\code{database_label} column nor attribute.}
}
\value{
A ggplot object, carrying its source table in the \code{gs_source}
attribute for \code{\link[=gs_save]{gs_save()}}.
}
\description{
One dotplot with arguments where the toolkit used to have eight functions.
Pathways run down the y axis; the statistic (or the leading-edge gene ratio)
runs along x; dot area is \code{-log10(padj)}; dot fill is the signed statistic on
a colorblind-safe diverging scale.
}
\details{
\strong{Selection and highlighting are separate}, which is the behaviour the old
\code{gsea_dotplot()} established and worth keeping: \code{top_n} and \code{sort_by} decide
\emph{what is shown}, \code{highlight} decides \emph{what gets a black outline}. A pathway
can be displayed without being significant.

The axis label for the statistic comes from the object's \code{stat_type}, so a
GSVA t-statistic is never mislabelled \code{NES}. Facet strips and legends show
the database \emph{label}, never the snake_case \code{database} key.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "SET_A", SET_B = "SET_B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
gs_plot_dot(res, top_n = 5)
}
