% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated-plot.R
\name{gsea_dotplot}
\alias{gsea_dotplot}
\title{Enhanced GSEA dotplot (deprecated)}
\usage{
gsea_dotplot(
  gsea_obj,
  filterBy = "p.adjust",
  sortBy = "GeneRatio",
  showCategory = 10,
  padj_cutoff = 0.05,
  title = "GSEA Dotplot",
  wrap_width = 50,
  neg_color = "#2166AC",
  mid_color = "#F7F7F7",
  pos_color = "#B35806",
  min.dotSize = 2,
  max.dotSize = 10,
  highlight_sig = TRUE,
  highlight_threshold = NULL,
  strip_prefix = TRUE,
  use_gradient = TRUE,
  nes_limits = NULL
)
}
\arguments{
\item{gsea_obj}{GSEA result object: a \code{gseaResult}, or a \code{gs_result}
(e.g. from the \code{run_gsea()} shim).}

\item{filterBy}{Method to sort/filter results: \code{"p.adjust"} (default),
\code{"NES"}, \code{"NES_positive"}, \code{"NES_negative"}.}

\item{sortBy}{Secondary display sort (\code{"GeneRatio"} or \code{"p.adjust"}).}

\item{showCategory}{Number of top pathways to show.}

\item{padj_cutoff}{Significance threshold for highlighting.}

\item{title}{Plot title.}

\item{wrap_width}{Width for text wrapping.}

\item{neg_color}{Colour for negative NES.}

\item{mid_color}{Colour for zero NES.}

\item{pos_color}{Colour for positive NES.}

\item{min.dotSize}{Minimum dot size.}

\item{max.dotSize}{Maximum dot size.}

\item{highlight_sig}{Whether to highlight significant points.}

\item{highlight_threshold}{FDR threshold for highlighting; \code{NULL} uses
\code{padj_cutoff}.}

\item{strip_prefix}{Logical, strip common prefixes like \code{"HALLMARK_"}.}

\item{use_gradient}{Logical, use continuous gradient for NES.}

\item{nes_limits}{Numeric vector of length 2 for symmetric NES limits.}
}
\value{
A ggplot object, as returned by \code{\link[=gs_plot_dot]{gs_plot_dot()}}.
}
\description{
Deprecated: use \code{\link[=gs_plot_dot]{gs_plot_dot()}} instead. This shim reproduces the old
formals of \code{gsea_dotplot()} verbatim and forwards to \code{gs_plot_dot()}.
}
\note{
The x axis carries the leading-edge \strong{gene ratio}, via
\code{gs_plot_dot(aes_x = "gene_ratio")}, because that is what the old dotplot
plotted (\code{GeneRatio <- count / setSize}); NES is the fill gradient. Leaving
the new renderer's \code{aes_x = "stat"} default in place silently changed what
the x axis meant.

\code{sortBy} (the old \emph{secondary} display sort) has no counterpart in
\code{gs_plot_dot()}, which orders the y axis by the chosen \code{sort_by} within the
selection. Accepted and ignored.

\code{use_gradient = FALSE} (the old "binary colour" mode) has no
counterpart: \code{gs_plot_dot()} always uses the continuous diverging
gradient. Accepted and ignored; the plot always renders with a gradient.
}
