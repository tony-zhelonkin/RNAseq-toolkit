% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-test.R
\name{gs_test}
\alias{gs_test}
\alias{gs_test.default}
\alias{gs_test.numeric}
\alias{gs_test.integer}
\alias{gs_test.character}
\alias{gs_test.gs_matrix}
\title{Test gene sets}
\usage{
gs_test(x, db, method = NULL, contrast = "contrast", ..., verbose = FALSE)

\method{gs_test}{default}(x, db, method = NULL, contrast = "contrast", ..., verbose = FALSE)

\method{gs_test}{numeric}(x, db, method = NULL, contrast = "contrast", ..., verbose = FALSE)

\method{gs_test}{integer}(x, db, method = NULL, contrast = "contrast", ..., verbose = FALSE)

\method{gs_test}{character}(x, db, method = NULL, contrast = "contrast", ..., verbose = FALSE)

\method{gs_test}{gs_matrix}(
  x,
  db = NULL,
  method = NULL,
  contrast = "contrast",
  ...,
  verbose = FALSE
)
}
\arguments{
\item{x}{A named numeric vector, a character vector of gene identifiers, or
a \code{gs_matrix}.}

\item{db}{A \code{gs_db} (see \verb{gsdb_*}), or a named list of \code{gs_db} objects; the
list name becomes the \code{database} column. Not used when \code{x} is a
\code{gs_matrix}, whose sets were already applied by \code{\link[=gs_score]{gs_score()}}.}

\item{method}{Optional character, \code{"fgsea"}, \code{"ora"} or \code{"limma"}. Only
needed to override the class-based choice.}

\item{contrast}{Character label recorded in the \code{contrast} column.}

\item{...}{Passed to the adapter: see \link{gs_test_fgsea_params} for the fgsea
and ORA knobs, and the limma arguments \code{design}, \code{coef}, \code{block}.}

\item{verbose}{Logical. Emit progress messages.}
}
\value{
A \link{gs_result}.
}
\description{
One verb for every gene-set test in the package. The method is chosen by the
class of \code{x}, so the common cases need no \code{method} argument at all:
}
\details{
\describe{
\item{named numeric vector}{a ranked statistic (see \code{\link[=gs_ranks]{gs_ranks()}}) ->
preranked GSEA via \code{\link[fgsea:fgseaMultilevel]{fgsea::fgseaMultilevel()}}, \code{stat_type = "NES"}.}
\item{character vector}{a gene list -> over-representation analysis via
\code{\link[fgsea:fora]{fgsea::fora()}}, \code{stat_type = "log2_fold_enrichment"}.}
\item{\code{gs_matrix}}{per-sample scores (see \code{\link[=gs_score]{gs_score()}}) -> limma over the
score matrix, \code{stat_type = "t"}.}
}

Every branch returns the same \link{gs_result}. Pooling across contrasts is
\code{rbind()} on those results, because \code{contrast} is a column -- there is no
separate pooled code path.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "Set A", SET_B = "Set B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
ranks <- stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6])
gs_test(ranks, db, min_size = 1, max_size = 10)
}
