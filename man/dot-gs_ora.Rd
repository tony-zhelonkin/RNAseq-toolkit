% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-test.R
\name{.gs_ora}
\alias{.gs_ora}
\title{ORA adapter}
\usage{
.gs_ora(genes, sets, universe = NULL, min_size = 10L, max_size = 500L)
}
\arguments{
\item{genes}{Character vector of query genes.}

\item{sets}{Named list of character vectors.}

\item{universe}{Character vector of background genes. Defaults to the union
of \code{genes} and every gene in \code{sets} -- state it explicitly whenever the
real background is the set of detected genes, which it usually is.}

\item{min_size, max_size}{Integer set-size bounds, applied after intersecting
with the data. Defaults \code{10} and \code{500} -- the values
\code{clusterProfiler::GSEA()} forwarded to fgsea, kept so results are
unchanged.}
}
\value{
A data frame of core \link{gs_result} columns.
}
\description{
ORA adapter
}
\keyword{internal}
