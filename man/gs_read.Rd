% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-io.R
\name{gs_read}
\alias{gs_read}
\title{Read a \code{gs_result} back from the standard table layout}
\usage{
gs_read(dir, name = "gsea")
}
\arguments{
\item{dir}{Directory written by \code{\link[=gs_write]{gs_write()}}, or a single \code{.tsv} path.}

\item{name}{Character stem used when writing.}
}
\value{
A \link{gs_result}.
}
\description{
Reads \verb{_overview/<name>_all.tsv} when it exists, otherwise every
\verb{by_contrast/*/<name>_*.tsv} and row-binds them. Also accepts a path to a
single \code{.tsv} file.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "Set A", SET_B = "Set B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
d <- file.path(tempdir(), "tables2")
gs_write(res, d)
gs_read(d)
}
