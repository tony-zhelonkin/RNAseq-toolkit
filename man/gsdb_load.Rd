% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsdb-load.R
\name{gsdb_load}
\alias{gsdb_load}
\title{Load a bundled gene-set database}
\usage{
gsdb_load(
  database,
  species = "Mus musculus",
  min_size = NULL,
  max_size = NULL,
  rebuild = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{database}{Character vector of database keys; see \code{\link[=gsdb_list]{gsdb_list()}}. A
vector of length > 1 returns a named list of \code{gs_db} objects.}

\item{species}{Character(1) species, \code{"Mus musculus"} or
\code{"Homo sapiens"}. The underscore form is accepted too.}

\item{min_size, max_size}{Integer(1) or \code{NULL}; drop sets outside these
bounds. \code{NULL} (the default) applies no bound.}

\item{rebuild}{Logical(1). Re-parsing raw source files requires a git
checkout of the toolkit; \code{TRUE} therefore always errors in an installed
package.}

\item{verbose}{Logical(1); message what was loaded.}
}
\value{
A \code{gs_db}, or a named list of \code{gs_db} when \code{database} has length
greater than one.
}
\description{
Loads one or more of the processed reference databases that ship with the
package and returns them as \code{\link[=gs_db]{gs_db()}} objects. Paths resolve only through
\code{system.file("extdata", ..., package = "bulkiRNA")}.
}
\examples{
db <- gsdb_load("mitopathways")
length(db)
}
