% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-ops.R
\name{gs_leading_edge}
\alias{gs_leading_edge}
\title{Extract leading-edge (or overlap) genes}
\usage{
gs_leading_edge(x, padj = NULL, top_n = NULL, unique_genes = FALSE)
}
\arguments{
\item{x}{A \link{gs_result}.}

\item{padj}{Numeric. Keep only pathways below this FDR; \code{NULL} keeps all.}

\item{top_n}{Integer. Keep only the top \code{top_n} pathways by \code{padj}.}

\item{unique_genes}{Logical. Return one pooled, de-duplicated character
vector instead of a per-pathway list.}
}
\value{
A named list of character vectors (names are \code{pathway_id}), or a
single character vector when \code{unique_genes = TRUE}.
}
\description{
Replaces \code{get_pathway_genes()}, \code{get_pathway_genes_all()} and
\code{get_significant_pathways()}: one function over the normalized result,
instead of three over an S4 \code{gseaResult}. ORA results carry their overlap
genes in the same \code{leading_edge} column.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "Set A", SET_B = "Set B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
gs_leading_edge(res)
}
