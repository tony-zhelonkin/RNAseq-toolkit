% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.require_pkg}
\alias{.require_pkg}
\title{Require a Suggests package, with a caller-supplied install hint}
\usage{
.require_pkg(pkg, what, install = sprintf("install.packages(\\"\%s\\")", pkg))
}
\arguments{
\item{pkg}{Character(1) package name.}

\item{what}{Character(1) description of what needed it.}

\item{install}{Character(1) install-command hint shown in the error.
Defaults to \code{install.packages("<pkg>")}.}
}
\value{
\code{TRUE}, invisibly; errors if \code{pkg} is not installed.
}
\description{
Shared body for the "this needs an optional package" check. Two other
near-duplicates of this existed (\code{.de_require()} in \code{R/de-utils.R} and
\code{.gatom_require()} in \code{R/gatom.R}, the latter hardcoding a
\code{BiocManager::install("gatom")} special case) plus several bare
\code{requireNamespace()} calls scattered around \verb{R/}. This is the union of
their behaviour: \code{install} is a caller-supplied hint string (as in
\code{.de_require()}), so call sites needing a \code{BiocManager::install(...)}
message (as \code{.gatom_require()} special-cased for \code{pkg == "gatom"}) just
pass that string instead of relying on a hardcoded branch.
}
\keyword{internal}
