% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-pca.R
\name{de_pca_3d}
\alias{de_pca_3d}
\title{Interactive three-dimensional PCA of a DGEList}
\usage{
de_pca_3d(
  dge,
  colour_by = "group",
  symbol_by = NULL,
  title = "3D PCA Plot",
  point_size = 8
)
}
\arguments{
\item{dge}{A \code{DGEList}, e.g. from \code{\link[=build_dge]{build_dge()}}.}

\item{colour_by}{Name of a \code{dge$samples} column mapped to marker colour, or
\code{NULL}.}

\item{symbol_by}{Name of a \code{dge$samples} column mapped to marker symbol, or
\code{NULL}.}

\item{title}{Plot title.}

\item{point_size}{Marker size.}
}
\value{
A \code{plotly} object.
}
\description{
The 3D counterpart of \code{\link[=de_pca]{de_pca()}}, rendered with plotly. Needs at least three
principal components, so at least three samples and three variable genes.
}
\examples{
if (requireNamespace("edgeR", quietly = TRUE) &&
    requireNamespace("plotly", quietly = TRUE)) {
  set.seed(1)
  counts <- matrix(rpois(600, 50), nrow = 100,
    dimnames = list(paste0("G", 1:100), paste0("S", 1:6)))
  samples <- data.frame(group = rep(c("WT", "KO"), each = 3),
                        row.names = colnames(counts))
  dge <- build_dge(counts, samples, data.frame(gene = rownames(counts)))
  de_pca_3d(dge, colour_by = "group")
}
}
