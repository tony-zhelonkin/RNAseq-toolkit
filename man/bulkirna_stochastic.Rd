% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rng.R
\name{bulkirna_stochastic}
\alias{bulkirna_stochastic}
\title{List stochastic bulkiRNA functions}
\usage{
bulkirna_stochastic(quiet = FALSE)
}
\arguments{
\item{quiet}{Logical. If \code{FALSE}, print the registry and return it
invisibly. If \code{TRUE}, do not print and return it visibly.}
}
\value{
A tibble with columns \code{name}, \code{seed_arg}, \code{seed_default},
\code{source_of_randomness}, and \code{note}.
}
\description{
Reports the public functions whose results consume randomness, the route by
which each function accepts a seed, and the default preserved by this
package version. \code{seed_arg} is \code{NA} when the seed is accepted only through
\code{...}; see the row's \code{note} for the documented parameter source.
}
\details{
The defaults deliberately differ. They are historical reproducibility
commitments, not a package-wide seed policy.
}
\examples{
stochastic <- bulkirna_stochastic(quiet = TRUE)
stochastic[, c("name", "seed_default")]
}
