% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-heatmap.R
\name{gs_plot_heatmap.gs_matrix}
\alias{gs_plot_heatmap.gs_matrix}
\title{Heatmap of a \code{gs_matrix}}
\usage{
\method{gs_plot_heatmap}{gs_matrix}(
  x,
  top = 30,
  samples = NULL,
  group = NULL,
  limits = NULL,
  colours = .gs_diverging_colours(),
  wrap_width = 40,
  strip_prefix = TRUE,
  title = NULL,
  ...
)
}
\arguments{
\item{x}{A \link{gs_matrix} of pathway scores.}

\item{top}{Number of pathways to display, chosen by score variance across
samples. \code{NULL} shows all.}

\item{samples}{Character vector selecting and ordering the samples, or
\code{NULL} for the matrix order.}

\item{group}{Name of a column in the matrix's \code{sample_data} to facet the
columns by, or \code{NULL}.}

\item{limits}{Length-2 numeric fill limits. \code{NULL} (default) derives
symmetric limits \strong{from this figure's own data}, so the same statistic can
take a different colour in two figures; pass an explicit \code{limits} when
panels are meant to be compared. The old renderer used a fixed
\code{c(-3.5, 3.5)}. Values outside are squished, not dropped.}

\item{colours}{Length-3 character vector -- low, mid, high fill colours.}

\item{wrap_width}{Soft character width for wrapping pathway labels.}

\item{strip_prefix}{Logical, passed to \code{\link[=format_pathway_name]{format_pathway_name()}}.}

\item{title}{Plot title, or \code{NULL}.}

\item{...}{Ignored.}
}
\value{
A ggplot object.
}
\description{
Heatmap of a \code{gs_matrix}
}
