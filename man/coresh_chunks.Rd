% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coresh.R
\name{coresh_chunks}
\alias{coresh_chunks}
\title{Index the CoReSh chunk tree}
\usage{
coresh_chunks(chunk_dir = NULL, species = "human", cache = TRUE)
}
\arguments{
\item{chunk_dir}{Optional explicit path to an \code{hsa} or \code{mmu} chunk directory.
When \code{NULL}, resolve the corresponding directory in the shared refcache.}

\item{species}{A human or mouse alias accepted by \code{\link[=.species]{.species()}}.}

\item{cache}{Logical. Reuse an index built earlier in this R session.}
}
\value{
A tibble with columns \code{gse}, \code{gpl}, and \code{chunk}. Its \code{provenance}
attribute records the resolved reference snapshot.
}
\description{
Reads every chunk for one species and returns one row per GEO dataset,
including the chunk file containing it. The index is cached in the package
namespace by normalized directory path; set \code{cache = FALSE} to rebuild it.
}
\examples{
\dontrun{
index <- coresh_chunks(species = "human")
}
}
