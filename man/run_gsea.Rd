% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-fixtures-gs.R
\name{run_gsea}
\alias{run_gsea}
\title{Deprecated: run preranked GSEA against an MSigDB collection}
\usage{
run_gsea(
  DE_results,
  rank_metric = "t",
  species = "Mus musculus",
  db_species = NULL,
  collection = "H",
  subcollection = "",
  pvalue_cutoff = 1,
  padj_method = "fdr",
  nperm = 1e+05,
  seed = 123
)
}
\arguments{
\item{DE_results}{A data frame of DE results. Must have gene identifiers
as rownames and a column named by \code{rank_metric}.}

\item{rank_metric}{Character. Column of \code{DE_results} to rank by (default:
\code{"t"}).}

\item{species}{Character. Species for MSigDB gene sets (default:
\code{"Mus musculus"}).}

\item{db_species}{Character. MSigDB source database (\code{"MM"}/\code{"HS"}), or
\code{NULL} (default) for the old "legacy" mode, which this shim maps to
\code{"HS"} -- the same human-database-with-ortholog-mapping behaviour the
old species-only branch used.}

\item{collection}{Character. MSigDB collection code (default: \code{"H"}).}

\item{subcollection}{Character. MSigDB subcollection code (default: \code{""},
meaning "no subcollection"; mapped to \code{NULL} for \code{\link[=gsdb_msigdb]{gsdb_msigdb()}}).}

\item{pvalue_cutoff}{Numeric. Nominal p-value cutoff applied to the result
after testing (default: 1, meaning no filtering).}

\item{padj_method}{Character. Accepted and ignored: \code{\link[=gs_test]{gs_test()}}'s fgsea
adapter always adjusts with \code{"BH"} (what \code{"fdr"} aliases to in
\code{\link[stats:p.adjust]{stats::p.adjust()}}), so a mismatched value only warns.}

\item{nperm}{Integer. Forwarded to \code{\link[=gs_test]{gs_test()}} as \code{n_perm_simple} (default:
100000).}

\item{seed}{Integer. Random seed set before testing (default: 123).}
}
\value{
A \code{\link[=gs_result]{gs_result()}} with \code{ranks} and \code{gene_sets} attributes set.
}
\description{
Superseded by \code{\link[=gs_ranks]{gs_ranks()}} + \code{\link[=gs_test]{gs_test()}}. The old \code{run_gsea()} returned a
\code{clusterProfiler} S4 \code{gseaResult}, which carried both the ranked vector
(\verb{@geneList}) and the tested gene sets (\verb{@geneSets}) alongside its results
table. A \code{\link[=gs_result]{gs_result()}} carries neither, so this shim attaches them as the
\code{ranks} and \code{gene_sets} attributes that \code{\link[=gs_plot_running]{gs_plot_running()}} falls back to
when its own \code{ranks}/\code{db} arguments are absent -- do not drop this when
touching this function again.
}
\keyword{internal}
