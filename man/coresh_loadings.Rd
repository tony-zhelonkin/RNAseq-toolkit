% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coresh-sets.R
\name{coresh_loadings}
\alias{coresh_loadings}
\title{Extract gene loadings for one CoReSh hit}
\usage{
coresh_loadings(chunk_path, gse_id, query, n_top = 50L, gpl = NULL)
}
\arguments{
\item{chunk_path}{Path to one \verb{*_full_objects.qs2} CoReSh chunk.}

\item{gse_id}{A single GEO series accession present in the chunk.}

\item{query}{A non-empty integer vector of Entrez IDs. Duplicate IDs are
removed with a message before loading extraction.}

\item{n_top}{Positive whole number of loadings to retain.}

\item{gpl}{Optional GEO platform accession. When omitted and the GSE occurs
on more than one platform in the chunk, the first platform in
locale-independent radix order is used with a warning.}
}
\value{
A tibble with \code{gse}, \code{gpl}, \code{entrez}, signed \code{loading}, and
absolute-loading \code{rank}, ordered from largest to smallest absolute
loading.
}
\description{
Projects every gene in a CoReSh dataset onto the unit-normalized profile of
the query genes and returns the genes with the largest absolute loadings.
Query IDs and duplicated reference IDs are matched with \code{\link[base:match]{base::match()}}, so
each Entrez ID contributes at most its first reference row, matching the
CoReSh search calculation. Missing reference Entrez IDs remain eligible for
the loading calculation but are omitted later when loadings are mapped to
symbols by \code{\link[=coresh_sets]{coresh_sets()}}.
}
\examples{
\dontrun{
coresh_loadings(
  "001_full_objects.qs2", "GSE123", c(1L, 2L, 3L), gpl = "GPL570"
)
}
}
