% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-score.R
\name{gs_score}
\alias{gs_score}
\title{Score gene sets per sample}
\usage{
gs_score(
  expr,
  db,
  method = c("gsva", "ssgsea", "zscore", "plage"),
  min_size = 10L,
  max_size = 500L,
  kcdf = c("Gaussian", "Poisson", "none"),
  sample_data = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{expr}{A numeric matrix of expression values (genes x samples), rows
named by the same identifier type as \code{db}. Log-scale values (voom logCPM,
vst, logTPM) are what \code{kcdf = "Gaussian"} expects.}

\item{db}{A \code{gs_db}.}

\item{method}{One of \code{"gsva"}, \code{"ssgsea"}, \code{"zscore"}, \code{"plage"}. Selects
the GSVA parameter object; it also becomes the matrix's \code{score_type}.}

\item{min_size, max_size}{Integer set-size bounds after intersecting with the
rows of \code{expr}.}

\item{kcdf}{Kernel for the \code{"gsva"} method: \code{"Gaussian"} for log-scale
continuous data, \code{"Poisson"} for integer counts.}

\item{sample_data}{Optional data frame of sample metadata, one row per
column of \code{expr}; \code{\link[=gs_test]{gs_test()}} builds its design from this.}

\item{verbose}{Logical. Passed to GSVA and used for progress messages.}

\item{...}{Additional arguments passed to the GSVA parameter constructor.}
}
\value{
A \link{gs_matrix}.
}
\description{
Turns an expression matrix into a pathway x sample \link{gs_matrix} with GSVA.
Feed the result to \code{\link[=gs_test]{gs_test()}} to get a \link{gs_result} with
\code{stat_type = "t"} — that pair is the GSVA -> limma pipeline, written once.
}
\details{
GSVA is a \code{Suggests}; the call is guarded.
}
\examples{
\dontrun{
sc  <- gs_score(logcpm, db, method = "gsva", sample_data = meta)
res <- gs_test(sc, design = ~ 0 + group, contrast = "groupKO-groupWT")
}
}
