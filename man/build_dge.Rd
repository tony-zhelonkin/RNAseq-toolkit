% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dge.R
\name{build_dge}
\alias{build_dge}
\title{Build a normalized DGEList}
\usage{
build_dge(
  count_mat,
  samples_df,
  genes_df,
  round_nonint = TRUE,
  norm_method = "TMM"
)
}
\arguments{
\item{count_mat}{Numeric matrix or data frame of counts, genes in rows.}

\item{samples_df}{Sample metadata; \code{rownames()} must equal
\code{colnames(count_mat)} in the same order.}

\item{genes_df}{Gene annotation with one row per row of \code{count_mat}, in the
same order.}

\item{round_nonint}{Logical. Round non-integer counts (with a message) or
stop.}

\item{norm_method}{Normalisation method passed to
\code{edgeR::calcNormFactors()}.}
}
\value{
A \code{DGEList} with normalisation factors computed.
}
\description{
Wraps \code{edgeR::DGEList()} + \code{edgeR::calcNormFactors()} with the two checks
that catch the mistakes that actually happen: non-integer counts (edgeR
silently misbehaves on them, so they are rounded with a message or refused)
and sample/gene tables that are out of order with the count matrix.
}
\examples{
if (requireNamespace("edgeR", quietly = TRUE)) {
  counts <- matrix(1:12, nrow = 3,
    dimnames = list(paste0("G", 1:3), paste0("S", 1:4)))
  samples <- data.frame(group = c("a", "a", "b", "b"),
                        row.names = colnames(counts))
  build_dge(counts, samples, data.frame(gene = rownames(counts)))
}
}
