% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{ensure_dir}
\alias{ensure_dir}
\title{Ensure a directory exists}
\usage{
ensure_dir(path)
}
\arguments{
\item{path}{Character vector of directory paths.}
}
\value{
\code{path}, invisibly, so the call can be inlined.
}
\description{
Creates each directory in \code{path} if it is missing, including parents.
Idempotent. \code{path} is a \strong{directory} path, never a file path — if you have a
file path, pass \code{dirname(path)} (or use the internal \code{\link[=ensure_parent_dir]{ensure_parent_dir()}}).
}
\examples{
d <- file.path(tempdir(), "figures")
ensure_dir(d)
dir.exists(d)
}
