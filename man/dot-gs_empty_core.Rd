% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-test.R
\name{.gs_empty_core}
\alias{.gs_empty_core}
\title{A zero-row core-column data frame}
\usage{
.gs_empty_core(
  numeric_cols = character(0),
  integer_cols = character(0),
  list_cols = character(0)
)
}
\arguments{
\item{numeric_cols, integer_cols, list_cols}{Optional column names to append,
zero-length and of the named type.}
}
\value{
A zero-row data frame with the core \link{gs_result} columns.
}
\description{
An empty result is a valid answer; \code{NULL} is not.
}
\details{
Adapters name their optional columns here so an empty result has the same
\emph{shape} as a non-empty one from the same method. Otherwise a contrast that
happened to yield no pathways lost \code{leading_edge}, and a per-contrast branch
calling \code{\link[=gs_leading_edge]{gs_leading_edge()}} got "carries no \code{leading_edge} column" -- a usage
error reported for what is simply an empty answer.
}
\keyword{internal}
