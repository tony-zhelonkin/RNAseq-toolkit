% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-result.R
\name{.gs_reclass_result}
\alias{.gs_reclass_result}
\title{Re-attach the \code{gs_result} class only if the contract still holds}
\usage{
.gs_reclass_result(x)
}
\arguments{
\item{x}{A data frame produced by a subset or a dplyr verb.}
}
\value{
A \code{gs_result} if valid, else a plain tibble.
}
\description{
\code{[} and every dplyr verb used to re-attach the class after checking column
\emph{presence} alone, so \code{validate_gs_result()} was only ever reached from
\code{gs_result()} itself. \code{mutate(res, padj = "oops")} or a restored \code{"Up"}/
\code{"Down"} capitalisation therefore produced an object that still claimed to be
a \code{gs_result}, and \code{gs_filter(direction = "up")} then returned zero rows
silently. Failing the contract downgrades to a plain tibble -- with a
warning, because a silent downgrade is the same class of surprise.
}
\keyword{internal}
