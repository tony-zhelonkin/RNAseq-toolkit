% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coresh-sets.R
\name{coresh_sets}
\alias{coresh_sets}
\title{Build gene sets from ranked CoReSh hits}
\usage{
coresh_sets(
  top_hits,
  queries,
  chunk_dir = NULL,
  species = "human",
  n_top = 50L,
  min_size = 15L,
  max_size = 500L,
  jaccard_threshold = 0.8,
  verbose = FALSE
)
}
\arguments{
\item{top_hits}{A data frame returned by \code{\link[=coresh_search]{coresh_search()}}, usually filtered
to the desired number of hits per query. Required columns are
\code{query_name}, \code{gse}, and \code{rank}; \code{gpl} selects the exact dataset when
present. For a historical table without \code{gpl}, an ambiguous accession
warns and uses the radix-first available platform.}

\item{queries}{A non-empty named list of integer Entrez vectors.}

\item{chunk_dir}{Optional explicit path to an \code{hsa} or \code{mmu} chunk directory.}

\item{species}{One of \code{"human"}, \code{"hsa"}, \code{"mouse"}, or \code{"mmu"}.}

\item{n_top}{Positive whole number of absolute loadings to retain per hit.}

\item{min_size, max_size}{Positive whole-number set-size bounds.}

\item{jaccard_threshold}{Numeric threshold in \verb{[0, 1]}. A later, lower
priority set is removed when overlap is strictly greater than this value.}

\item{verbose}{Logical. Report the reason for each failed hit in addition to
the always-reported failure count.}
}
\value{
A \code{\link[=gs_db]{gs_db()}} with database-level \code{provenance} and a set-keyed
\code{set_provenance} tibble. The latter contains \code{set_name}, \code{query_name},
\code{gse}, \code{gpl}, \code{chunk_path}, \code{loading_cutoff}, and \code{rank_in_coresh}.
}
\description{
For every hit, \code{\link[=coresh_loadings]{coresh_loadings()}} extracts the strongest gene loadings and
\code{\link[=entrez_to_gene]{entrez_to_gene()}} maps them to symbols. Sets outside the size bounds are
dropped. Remaining sets are ordered explicitly by CoReSh rank, query name,
GSE, and platform before Jaccard de-duplication; therefore a more highly
ranked hit wins, rather than whichever row the caller happened to supply
first.
}
\details{
Failures are counted separately from valid empty results. A partial run
reports how many hits failed and returns its successful sets. If every
attempted hit fails, the function stops instead of returning an apparently
clean empty database.
}
\examples{
\dontrun{
db <- coresh_sets(
  hits[hits$rank <= 5L, ],
  list(iron = c(7037L, 4891L, 55240L)),
  species = "human"
)
}
}
