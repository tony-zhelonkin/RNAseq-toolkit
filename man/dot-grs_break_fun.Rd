% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-running.R
\name{.grs_break_fun}
\alias{.grs_break_fun}
\title{Break function that restores original units panel by panel}
\usage{
.grs_break_fun(win)
}
\arguments{
\item{win}{The output of \code{\link[=.grs_windows]{.grs_windows()}}.}
}
\value{
A function suitable for \code{scale_y_continuous(breaks = )}.
}
\description{
With \code{scales = "free_y"} every panel resolves its own breaks and a break
function gets no panel context -- but each panel's limits are exactly
\code{c(0, h)} (pinned by \code{geom_blank()} with zero expansion, heights made
distinct in \code{\link[=.grs_windows]{.grs_windows()}}), so the panel is identifiable from its limits
alone. Breaks are chosen in original units, mapped into window space, and
returned \emph{named} with their original-unit labels. The tick panel's y index
is meaningless, so it gets no breaks at all.
}
\keyword{internal}
