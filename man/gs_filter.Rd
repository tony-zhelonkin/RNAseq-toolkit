% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-ops.R
\name{gs_filter}
\alias{gs_filter}
\title{Filter a \code{gs_result}}
\usage{
gs_filter(
  x,
  padj = NULL,
  p_value = NULL,
  stat = NULL,
  direction = NULL,
  database = NULL,
  contrast = NULL,
  pathway_id = NULL,
  pattern = NULL,
  min_genes = NULL,
  max_genes = NULL
)
}
\arguments{
\item{x}{A \link{gs_result}.}

\item{padj}{Numeric. Keep rows with \code{padj < padj}.}

\item{p_value}{Numeric. Keep rows with \code{p_value < p_value}.}

\item{stat}{Numeric. Keep rows with \code{abs(stat) >= stat}.}

\item{direction}{Character subset of \code{"up"}, \code{"down"}, \code{"ns"}.}

\item{database, contrast}{Character vectors of values to keep.}

\item{pathway_id}{Character vector of pathway identifiers to keep.}

\item{pattern}{Regular expression matched against \code{pathway_name} and
\code{pathway_id}.}

\item{min_genes, max_genes}{Integer bounds on \code{n_genes_tested}.}
}
\value{
A \link{gs_result} with the surviving rows.
}
\description{
The named-argument filter every downstream script re-wrote by hand. Every
argument is optional and \code{NULL} means "no constraint", so
\code{gs_filter(res, padj = 0.05, direction = "up")} reads as the sentence it is.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "Set A", SET_B = "Set B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
gs_filter(res, padj = 0.5, direction = "up")
}
