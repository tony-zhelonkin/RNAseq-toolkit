% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-result.R
\name{gs_result-class}
\alias{gs_result-class}
\title{The \code{gs_result} contract}
\description{
\code{gs_result} is the single normalized shape every gene-set test returns,
whatever the method. It is a \link[tibble:tibble]{tibble} subclass with one row
per pathway per contrast.
}
\section{Core columns}{
Always present, always these types:

\describe{
\item{\code{pathway_id}}{character. Stable identifier, e.g. \code{"HALLMARK_HYPOXIA"}.}
\item{\code{pathway_name}}{character. Human-readable label for axes.}
\item{\code{database}}{character. Which \verb{gsdb_*} provider the set came from.}
\item{\code{contrast}}{character. Comparison label, e.g. \code{"KO-WT"}. Pooling
across contrasts is \code{rbind()}, not a separate code path.}
\item{\code{method}}{character. \code{"fgsea"}, \code{"ora"}, \code{"geseca"}, or -- for a
\link{gs_matrix} tested with limma -- the scoring method that produced the
matrix (\code{"gsva"}, \code{"ssgsea"}, \code{"zscore"}, \code{"plage"}).}
\item{\code{n_genes}}{integer. Set size in the database.}
\item{\code{n_genes_tested}}{integer. Set size after intersecting with the data.}
\item{\code{stat}}{numeric. The effect size. What it \emph{is} is named by \code{stat_type}.}
\item{\code{stat_type}}{character. One of \code{\link[=gs_stat_types]{gs_stat_types()}}.}
\item{\code{direction}}{character. \code{"up"}, \code{"down"}, or \code{"ns"}; \code{NA} when a
method's statistic is unsigned.}
\item{\code{p_value}}{numeric. Raw p-value.}
\item{\code{padj}}{numeric. Multiplicity-adjusted p-value.}
}
}

\section{Optional columns}{
Method-specific, dropped when not applicable: \code{es} (raw enrichment score),
\code{log2err} (the estimator's own bound on the log2 error of the p-value),
\code{leading_edge} (list of character vectors), \code{fold_enrichment}, \code{overlap}.
\code{log2err = Inf} means the estimate is past reliable resolution, not that the
computation failed.

Renderers label their statistic axis from \code{stat_type}, so a GSVA
t-statistic is never mislabelled "NES". Never write a \code{stat} column without
the matching \code{stat_type}.
}

