% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dge.R
\name{annotate_genes}
\alias{annotate_genes}
\title{Annotate Ensembl gene IDs with symbols, Entrez IDs and biotype}
\usage{
annotate_genes(
  ens_ids,
  species = c("Mus musculus", "Homo sapiens"),
  use_biomart = TRUE,
  input_gene_name = NULL,
  biomart_version = NULL,
  biomart_host = NULL
)
}
\arguments{
\item{ens_ids}{Character vector of Ensembl gene IDs, with or without
version suffixes.}

\item{species}{\code{"Mus musculus"} or \code{"Homo sapiens"}.}

\item{use_biomart}{Logical. Attempt the biomaRt refinement.}

\item{input_gene_name}{Optional character vector of symbols supplied by the
quantifier, either named by version-stripped ID (as produced by
\code{\link[=read_counts_matrix]{read_counts_matrix()}}) or parallel to \code{ens_ids}.}

\item{biomart_version}{Optional Ensembl release for \code{useEnsembl()}.}

\item{biomart_host}{Optional biomaRt host.}
}
\value{
A tibble with one row per element of \code{ens_ids} and columns \code{Symbol},
\code{Ensembl}, \code{ENTREZID}, \code{gene_biotype}, \code{input_gene_name}.
}
\description{
Maps version-stripped Ensembl gene IDs through the species \code{org.*.eg.db}
package, then -- when \code{use_biomart = TRUE} and the network is reachable --
refines the symbol and adds \code{gene_biotype} from Ensembl via biomaRt. The
biomaRt step is best effort: a failure leaves the org.db result intact
rather than aborting the annotation.
}
\details{
Genes with no symbol keep their stable Ensembl ID as \code{Symbol}, so the column
is never \code{NA} and downstream joins and plots do not silently lose rows.
}
\examples{
if (requireNamespace("org.Mm.eg.db", quietly = TRUE)) {
  annotate_genes(c("ENSMUSG00000000001.5", "ENSMUSG00000000003"),
                 use_biomart = FALSE)
}
}
