% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-save.R
\name{gs_save}
\alias{gs_save}
\title{Save a figure and its source table}
\usage{
gs_save(
  plot,
  path,
  width = 8,
  height = 6,
  dpi = 300,
  formats = c("pdf", "png"),
  data = NULL,
  table = TRUE
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{path}{Output path stem. A recognised extension (\code{.pdf}, \code{.png},
\code{.tsv}) is stripped; the parent directory is created if missing.}

\item{width, height}{Figure size in inches.}

\item{dpi}{Resolution for the raster output.}

\item{formats}{Character vector of image formats to write. Default
\code{c("pdf", "png")}.}

\item{data}{Source table to write instead of the plot's own. Pass \code{NULL}
(default) to use the plot's \code{gs_source} attribute, falling back to
\code{plot$data}.}

\item{table}{Logical. Write the source table. \code{FALSE} writes only images.}
}
\value{
The written paths, invisibly.
}
\description{
The craft rule "a figure's source table is its same-stem neighbour" is this
function's job, not something to remember. From one plot object \code{gs_save()}
writes, at the same stem:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{<stem>.pdf   <stem>.png   <stem>.tsv
}\if{html}{\out{</div>}}

The table is the frame the plot was drawn from -- the \code{gs_source} attribute
every \verb{gs_plot_*} renderer attaches -- or whatever you pass as \code{data}. List
columns are collapsed with \code{/}, matching \code{\link[=gs_write]{gs_write()}}.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "SET_A", SET_B = "SET_B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
p <- gs_plot_dot(res, top = 5)
gs_save(p, file.path(tempdir(), "figures", "demo_dot"))
}
