% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-master.R
\name{gs_validate_master}
\alias{gs_validate_master}
\title{Validate a master GSEA table}
\usage{
gs_validate_master(df, schema_version = "1", error = TRUE)
}
\arguments{
\item{df}{A data frame intended to satisfy the master-table schema.}

\item{schema_version}{Character(1) master-table schema version.}

\item{error}{Logical(1). Stop with all problems when \code{TRUE}; return the
problem tibble when \code{FALSE}.}
}
\value{
With \code{error = FALSE}, a tibble with \code{check}, \code{column}, and \code{message}
columns (zero rows when valid). With \code{error = TRUE}, \code{df} invisibly when
valid, otherwise an error.
}
\description{
Checks the versioned schema, exact column order and declared types, then
verifies master-table semantic invariants. With \code{error = TRUE}, every
detected problem is included in one error.
}
\examples{
ranks <- c(A = 2.5, B = 1.8, C = 0.9, D = -0.4, E = -1.2, F = -2.1)
db <- gsdb_register(list(SET_A = c("A", "B", "C")),
                    database = "demo", species = "Homo sapiens")
master <- gs_to_master(gs_test(ranks, db, min_size = 2))

gs_validate_master(master, error = FALSE)   # zero rows means clean

# The defect this exists to catch: a derived column NA-filled beside a
# finite `padj`, which is what a column allowlist plus rbind() produces.
broken <- master
broken$neg_log_padj <- NA_real_
gs_validate_master(broken, error = FALSE)
}
