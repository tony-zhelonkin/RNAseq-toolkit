% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsdb-rebuild.R
\name{.gsdb_rebuild}
\alias{.gsdb_rebuild}
\title{Rebuild the bundled processed reference files from raw sources}
\usage{
.gsdb_rebuild(
  species = "Mus musculus",
  refs_dir = file.path("data", "references"),
  min_size = 5L,
  max_size = 500L,
  write = TRUE
)
}
\arguments{
\item{species}{Character(1), \code{"Mus musculus"} or \code{"Homo sapiens"}.}

\item{refs_dir}{Character(1) path to the \code{data/references} directory of a
source checkout.}

\item{min_size, max_size}{Integer(1) size bounds applied to every database.}

\item{write}{Logical(1); write the processed RDS files under \code{refs_dir}.}
}
\value{
Named list of \code{gs_db} objects, invisibly.
}
\description{
The internal successor to \code{scripts/GSEA/GSEA_processing/build_reference_databases.R},
which executed at source time. This is a plain function: it parses the raw
files under \code{refs_dir}, applies the size filter and Jaccard
de-duplication, and writes the processed RDS files in the legacy
\code{list(T2G, T2N)} shape that \code{\link[=gsdb_load]{gsdb_load()}} reads. It is a maintainer tool,
not part of the user surface, and errors via
\code{\link[=.gsdb_rebuild_unavailable]{.gsdb_rebuild_unavailable()}} when the raw tree is absent.
}
\keyword{internal}
