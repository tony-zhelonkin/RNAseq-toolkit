% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-fixtures-plot.R
\name{gsea_running_sum_plot}
\alias{gsea_running_sum_plot}
\title{Unified GSEA running-sum plot (deprecated)}
\usage{
gsea_running_sum_plot(
  gsea_obj,
  gene_set_ids = NULL,
  palette = NULL,
  labels = NULL,
  legend_pos = c(0.98, 0.98),
  base_size = 14,
  max_name_length = 40,
  title = NULL,
  panel_heights = c(2.4, 0.7, 0.9),
  legend_position = c("inside", "right", "none"),
  es_ylim = NULL,
  xticks = c("bottom", "all"),
  rug_ylabels = FALSE,
  base_theme = NULL
)
}
\arguments{
\item{gsea_obj}{A \code{gseaResult} object from clusterProfiler/fgsea, or a
\code{gs_result} (e.g. from the \code{run_gsea()} shim) -- passed through
untouched, so its own \code{ranks}/\code{gene_sets} attributes drive the curve.}

\item{gene_set_ids}{Integer vector (row indices) or character vector
(pathway IDs). \code{NULL} (default) picks the new renderer's default top 5
by \eqn{|NES|}.}

\item{palette}{Optional colour vector, keyed as documented in
\code{\link[=gs_plot_running]{gs_plot_running()}}.}

\item{labels}{Optional legend labels, keyed as documented in
\code{\link[=gs_plot_running]{gs_plot_running()}}.}

\item{legend_pos}{Inside-panel legend position (used when
\code{legend_position = "inside"}).}

\item{base_size}{Base font size for the per-panel theme.}

\item{max_name_length}{Maximum character length for pathway names in the
legend.}

\item{title}{Optional title for the ES (top) panel.}

\item{panel_heights}{Length-3 numeric, the ES:rug:metric height ratio.}

\item{legend_position}{One of \code{"inside"} (default), \code{"right"} or \code{"none"}.}

\item{es_ylim}{Optional length-2 numeric; absorbed (see \verb{@note}).}

\item{xticks}{\code{"bottom"} (default) or \code{"all"}; absorbed (see \verb{@note}).}

\item{rug_ylabels}{Logical; absorbed (see \verb{@note}).}

\item{base_theme}{Optional complete ggplot2 theme used as the foundation
for every panel.}
}
\value{
A ggplot object, as returned by \code{\link[=gs_plot_running]{gs_plot_running()}}.
}
\description{
Deprecated: use \code{\link[=gs_plot_running]{gs_plot_running()}} instead. This shim reproduces the old
formals of \code{gsea_running_sum_plot()} verbatim.
}
\note{
\code{es_ylim} is absorbed and ignored: the new renderer would need a
per-facet zoom to honour it, which was deliberately dropped when
\code{gs_plot_running()} was written.

\code{xticks} and \code{rug_ylabels} are absorbed and ignored: the new
renderer's layout is now structurally equivalent to the old
\code{xticks = "bottom"}, \code{rug_ylabels = FALSE} combination (x ticks only on
the bottom panel; the rug panel's y-index labels are always blanked), so
both old values of each collapse onto that one good layout.

\code{gsea_obj@geneList} and \code{gsea_obj@geneSets} are attached to the
converted \code{gs_result} as the \code{ranks} and \code{gene_sets} attributes, which
\code{\link[=gs_plot_running]{gs_plot_running()}} falls back to when its own \code{ranks}/\code{db} arguments are
\code{NULL} -- so this shim never duplicates that lookup logic itself.
}
\keyword{internal}
