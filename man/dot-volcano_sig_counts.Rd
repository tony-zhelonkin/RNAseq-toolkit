% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-volcano.R
\name{.volcano_sig_counts}
\alias{.volcano_sig_counts}
\title{Up/down counts for the highest-priority populated legend category}
\usage{
.volcano_sig_counts(df)
}
\arguments{
\item{df}{Data frame carrying \code{logFC}, \code{sig_dec}, \code{sig_fc}.}
}
\value{
A list with \code{category}, \code{n_up}, \code{n_down}, \code{legend_key}.
}
\description{
Walks the colour categories in priority order and returns the up/down split
of the \strong{first} one that has any members, so the rendered counts always
belong to the strongest claim the legend actually makes:
}
\details{
\enumerate{
\item \code{"both"} — combined FDR & \verb{|log2FC|} (\code{sig_dec & sig_fc}).
\item \code{"fdr"} — FDR only (\code{sig_dec}), used when priority 1 is empty.
\item \code{"none"} — nothing crosses FDR; counts are \code{0 / 0}.
}

\code{legend_key} names the colour category whose legend label should receive the
appended count line. Up counts \code{logFC > 0}, down counts \code{logFC < 0}.
}
\keyword{internal}
