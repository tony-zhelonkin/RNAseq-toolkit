% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-io.R
\name{read_metadata}
\alias{read_metadata}
\title{Read a sample metadata sheet}
\usage{
read_metadata(
  path,
  sample_col_candidates = c("Sample_ID", "Sample ID"),
  required_cols = character(0)
)
}
\arguments{
\item{path}{Path to an Excel or delimited-text metadata file.}

\item{sample_col_candidates}{Column names tried in order for the sample-ID
column.}

\item{required_cols}{Character vector of further columns that must be
present; empty by default.}
}
\value{
A data frame with a \code{Sample_ID} column.
}
\description{
Reads \code{.xlsx}/\code{.xls} through readxl and anything else as delimited text,
then renames whichever of \code{sample_col_candidates} it finds to the canonical
\code{Sample_ID}. Additional required columns are opt-in, so a sheet from another
project loads without a project-specific schema error.
}
\examples{
f <- tempfile(fileext = ".csv")
write.csv(data.frame(Sample_ID = c("S1", "S2"), group = c("WT", "KO")),
          f, row.names = FALSE)
read_metadata(f)
}
