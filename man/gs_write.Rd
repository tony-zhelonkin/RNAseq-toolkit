% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-io.R
\name{gs_write}
\alias{gs_write}
\title{Write a \code{gs_result} to the standard table layout}
\usage{
gs_write(
  x,
  dir,
  name = "gsea",
  overview = TRUE,
  by_contrast = TRUE,
  prune = FALSE
)
}
\arguments{
\item{x}{A \link{gs_result}.}

\item{dir}{Directory to write into; created if missing.}

\item{name}{Character stem for the file names. Default \code{"gsea"}.}

\item{overview}{Logical. Also write the pooled table and the
\code{\link[=summary.gs_result]{summary.gs_result()}} table under \verb{_overview/}.}

\item{by_contrast}{Logical. Also write one file per contrast x database.}

\item{prune}{Logical. Delete the existing \verb{by_contrast/} tree before writing.
\code{gs_write()} otherwise only ever adds files, so re-running a stage with a
contrast dropped leaves the old directory in place for \code{\link[=gs_read]{gs_read()}} to pick
up. Off by default because it deletes data; a manifest is written either
way, and \code{gs_read()} warns about files that are not in it.}
}
\value{
The directory, invisibly, with the written paths in the \code{"files"}
attribute.
}
\description{
\code{gs_write()} and \code{\link[=gs_read]{gs_read()}} are the only functions in the compute layer
that touch disk. The layout under \code{dir} is the project convention:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{<dir>/
  by_contrast/<contrast>/<name>_<database>.tsv
  _overview/<name>_all.tsv
  _overview/<name>_summary.tsv
}\if{html}{\out{</div>}}

\code{dir} is normally \verb{03_results/<stage>/tables}. List columns
(\code{leading_edge}) are collapsed to \code{"/"}-separated strings on the way out and
split again by \code{\link[=gs_read]{gs_read()}}, so the round trip is lossless.

This layout is for \code{gs_result} only. A \link{gs_matrix} of per-sample scores has
no table format here -- persist it with \code{\link[=saveRDS]{saveRDS()}}, which keeps its
\code{sample_data} and score-type attributes as they are.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "Set A", SET_B = "Set B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
d <- file.path(tempdir(), "tables")
gs_write(res, d)
gs_read(d)
}
