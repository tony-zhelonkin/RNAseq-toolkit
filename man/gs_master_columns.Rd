% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-master.R
\name{gs_master_columns}
\alias{gs_master_columns}
\title{List master-table columns in emission order}
\usage{
gs_master_columns(schema_version = "1", optional = FALSE)
}
\arguments{
\item{schema_version}{Character(1) master-table schema version.}

\item{optional}{Whether to include the optional columns. \code{gs_to_master()}
emits them only when given an \code{entity_type}, so the default describes a
table built without one.}
}
\value{
A character vector of master-table column names in emission order.
}
\description{
Returns the columns declared by the versioned master-table schema in the
order \code{\link[=gs_to_master]{gs_to_master()}} emits them and \code{\link[=gs_validate_master]{gs_validate_master()}} expects.
Optional columns come first, followed by the required columns.
}
\details{
The shipped schema CSV lists \code{entity_type} last while \code{gs_to_master()} emits
it first, so take the order from here rather than from the file.
}
\examples{
gs_master_columns()
gs_master_columns(optional = TRUE)
}
