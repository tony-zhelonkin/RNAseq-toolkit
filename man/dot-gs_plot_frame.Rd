% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-utils.R
\name{.gs_plot_frame}
\alias{.gs_plot_frame}
\title{Build the plotting frame for a \code{gs_result}}
\usage{
.gs_plot_frame(
  x,
  top_n = NULL,
  sort_by = "padj",
  direction = "both",
  group_by = NULL,
  keep_ids = NULL,
  highlight = 0.05,
  wrap_width = 50,
  strip_prefix = TRUE,
  database_labels = NULL
)
}
\arguments{
\item{x}{A \code{gs_result}.}

\item{top_n}{Integer. Rows kept per selection group; \code{NULL} or \code{Inf} keeps
all.}

\item{sort_by}{One of \code{"padj"}, \code{"p_value"}, \code{"stat"} (absolute magnitude),
\code{"stat_signed"}.}

\item{direction}{One of \code{"both"}, \code{"up"}, \code{"down"}.}

\item{group_by}{Character vector of columns defining the selection groups,
or \code{NULL}.}

\item{keep_ids}{Character vector of \code{pathway_id}s. When supplied, rows are
restricted to these ids and \code{top_n} selection is skipped -- this is how
\verb{compare =} keeps every panel of a comparison grid on the same pathways.}

\item{highlight}{Numeric FDR threshold for the \code{significant} flag, or
\code{NULL}.}

\item{wrap_width}{Soft character width for label wrapping.}

\item{strip_prefix}{Logical, passed to \code{\link[=format_pathway_name]{format_pathway_name()}}.}

\item{database_labels}{Named character vector of display labels, or \code{NULL}.}
}
\value{
A data frame with columns \code{pathway_id}, \code{label}, \code{stat}, \code{p_value},
\code{padj}, \code{neg_log_padj}, \code{direction}, \code{database}, \code{database_label},
\code{contrast}, \code{significant}, and \code{gene_ratio} when available.
}
\description{
Selects rows, resolves display labels, and returns a plain data frame with
the columns every dot/bar/tile renderer needs. Selection is by \code{sort_by}
within \code{group_by}; ties keep the object's own row order.
}
\keyword{internal}
