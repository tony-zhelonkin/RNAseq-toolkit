% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-ops.R
\name{gs_top}
\alias{gs_top}
\title{Take the top pathways of a \code{gs_result}}
\usage{
gs_top(
  x,
  n = 10L,
  by = c("padj", "p_value", "stat"),
  by_direction = FALSE,
  per = c("database", "contrast")
)
}
\arguments{
\item{x}{A \link{gs_result}.}

\item{n}{Integer, how many rows to keep per group.}

\item{by}{What to rank by: \code{"padj"}, \code{"p_value"} (smallest first) or
\code{"stat"} (largest \code{abs(stat)} first).}

\item{by_direction}{Logical. Take \code{n} up and \code{n} down separately, which is
what a balanced dotplot needs.}

\item{per}{Character vector of grouping columns; \code{n} applies within each
group. Defaults to \code{c("database", "contrast")}.}
}
\value{
A \link{gs_result} with at most \code{n} rows per group (per direction, if
\code{by_direction}), ordered by the ranking column.
}
\description{
Selection, not filtering: \code{gs_top()} decides \emph{which rows are shown}, leaving
significance to the renderer's highlighting. That separation is the one the
old \code{gsea_dotplot()} documented and everything else forgot.
}
\examples{
db <- structure(
  list(SET_A = c("A", "B", "C", "D"), SET_B = c("C", "D", "E", "F")),
  pathway_names = c(SET_A = "Set A", SET_B = "Set B"),
  database = "demo", species = "Homo sapiens", gene_id_type = "symbol",
  class = "gs_db"
)
res <- gs_test(stats::setNames(c(3, 2, 1, -1, -2, -3), LETTERS[1:6]),
               db, min_size = 1, max_size = 10)
gs_top(res, n = 1)
}
