% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-theme.R
\name{dot-theme_bulki}
\alias{dot-theme_bulki}
\alias{.theme_bulki}
\title{\code{theme_bulki()} with the base-size floor as a parameter}
\usage{
.theme_bulki(base_size = 14, base_family = "", grid = FALSE, floor = 14)
}
\arguments{
\item{base_size}{Base font size in points. Values below \code{14} are raised to
\code{14}.}

\item{base_family}{Base font family. Default \code{""} (the device default).}

\item{grid}{Logical. \code{FALSE} (default) removes all grid lines; \code{TRUE} keeps
a light major grid on both axes.}

\item{floor}{Numeric(1) smallest honoured \code{base_size}, or \code{NULL} for none.}
}
\value{
A ggplot2 theme object.
}
\description{
The floor is deliberate for the public theme, but the deprecated
\code{custom_minimal_theme_with_grid()} shim has to reproduce the old theme
exactly, and the old theme had no floor -- its documented default was 12 pt
and it rendered at 12 pt. Routing both through this helper lets the public
function keep its floor while the shim passes \code{floor = NULL}, instead of
either silently enlarging every legacy figure or clamping in two places.
}
\keyword{internal}
