% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-coregulation.R
\name{gs_coregulation}
\alias{gs_coregulation}
\title{Test gene-set coregulation with GESECA}
\usage{
gs_coregulation(
  expr,
  db,
  center = TRUE,
  scale = FALSE,
  min_size = 10L,
  max_size = 500L,
  sample_size = 101L,
  eps = 1e-50,
  seed = 123L,
  verbose = FALSE
)
}
\arguments{
\item{expr}{Numeric matrix of expression values, genes x samples. Rows must
have unique, non-missing gene identifiers matching \code{db}; columns are
samples. A \link{gs_matrix} is rejected because its rows are pathways, not
genes.}

\item{db}{A \code{gs_db} (see \verb{gsdb_*}), or a named list of \code{gs_db} objects; the
list name becomes the \code{database} column.}

\item{center}{Logical. Center every gene across samples before testing.
Defaults to \code{TRUE}, as required for a general expression matrix.}

\item{scale}{Logical. Scale every gene to unit variance before testing.
Defaults to \code{FALSE} so genes retain their relative variation.}

\item{min_size, max_size}{Positive whole-number set-size bounds after
intersecting each set with the rows of \code{expr}.}

\item{sample_size}{Positive whole number controlling the precision of the
adaptive multilevel estimator. The default \code{101L} is fgsea's GESECA
default; larger values improve precision at additional cost.}

\item{eps}{Non-negative finite p-value boundary passed to
\code{\link[fgsea:geseca]{fgsea::geseca()}}. Use \code{0} to remove the boundary.}

\item{seed}{Finite whole-number RNG seed, or \code{NULL} to use the caller's RNG
stream. The default \code{123L} matches the package's general fgsea adapter;
CoReSh keeps its separate historical default of \code{1L}.}

\item{verbose}{Logical. Emit one progress message per database.}
}
\value{
A \link{gs_result} with \code{method = "geseca"},
\code{stat_type = "pct_var"}, \code{stat} equal to percentage of total variance
explained, \code{direction = NA}, and GESECA's \code{log2err} retained.
}
\description{
\code{gs_coregulation()} tests whether genes in each set vary together across
samples. Unlike \code{\link[=gs_test]{gs_test()}}, it takes a genes x samples expression matrix
and has no contrast. The result uses \code{contrast = "coregulation"} as the
required non-contrast grouping label in the \link{gs_result} contract.
}
\details{
Rows are centered by default because GESECA's variance-along-a-direction
score assumes zero-mean gene profiles. Scaling remains off: unit-variance
scaling would change the question from coregulation in the supplied
expression units to equal contribution from every gene. Set \code{center = FALSE}
only when the rows have already been centered, for example after a PCA
reduction or for a CoReSh expression chunk.

The statistic is unsigned, so \code{direction} is \code{NA} on every row. Filtering
this result to \code{direction = "up"}, \code{"down"}, or \code{"ns"} therefore returns no
rows; rank it by \code{stat}, \code{p_value}, or \code{padj} instead. Its
\code{\link[=summary.gs_result]{summary.gs_result()}} reports \code{n_up} and \code{n_down} as unavailable (\code{NA}).
Direction-restricted or direction-faceted plots are therefore not
meaningful; use the renderers' default \code{direction = "both"} and an
unsigned facet such as database or contrast.

A \code{pct_var} result cannot enter \code{\link[=gs_to_master]{gs_to_master()}} by default because the
master table's \code{nes} column is reserved for NES. Pass
\code{stat_as_nes = TRUE} only when deliberately asserting that percentage of
variance explained may populate that column, or keep the result out of the
master table.
}
\examples{
expr <- rbind(
  A = c(-2, -1, 1, 2),
  B = c(-1.8, -0.9, 0.9, 1.8),
  C = c(1, -1, 1, -1),
  D = c(-1, 1, -1, 1)
)
db <- gsdb_register(
  list(COREGULATED = c("A", "B"), SCRAMBLED = c("A", "C")),
  database = "demo", species = "Homo sapiens"
)
gs_coregulation(expr, db, min_size = 2L, max_size = 3L)
}
