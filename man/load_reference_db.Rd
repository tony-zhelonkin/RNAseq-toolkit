% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-fixtures-gs.R
\name{load_reference_db}
\alias{load_reference_db}
\title{Deprecated: load a bundled reference gene-set database}
\usage{
load_reference_db(
  database,
  species = "Mus_musculus",
  toolkit_dir = NULL,
  rebuild = FALSE
)
}
\arguments{
\item{database}{Character. Database key; see \code{\link[=gsdb_list]{gsdb_list()}}.}

\item{species}{Character. Species directory name (default:
\code{"Mus_musculus"}). Forwarded as-is; \code{\link[=gsdb_load]{gsdb_load()}} accepts both the
underscore and spaced forms.}

\item{toolkit_dir}{Character. Accepted and ignored, as in
\code{list_reference_dbs()}.}

\item{rebuild}{Logical. If \code{TRUE}, \code{\link[=gsdb_load]{gsdb_load()}} always errors: an
installed package never ships the raw source files a rebuild needs, even
when this shim happens to run from a source checkout. The old function
could actually rebuild in that situation; this is a documented behaviour
change, not a bug in the shim.}
}
\value{
List with \code{T2G}, \code{T2N}, \code{source}, \code{created}, matching the object
the pre-refactor \code{load_reference_db()} returned.
}
\description{
Superseded by \code{\link[=gsdb_load]{gsdb_load()}}. Returns the legacy \code{list(T2G =, T2N =, source =, created =)} shape a \code{clusterProfiler::GSEA()} call expects,
rebuilt from the \code{\link[=gs_db]{gs_db()}} that \code{\link[=gsdb_load]{gsdb_load()}} now returns.
}
\keyword{internal}
