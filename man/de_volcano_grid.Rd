% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-volcano.R
\name{de_volcano_grid}
\alias{de_volcano_grid}
\title{Combine volcano panels into one row with a shared scale and legend}
\usage{
de_volcano_grid(
  plots,
  labels = names(plots),
  legend_position = "bottom",
  keep_first_caption = FALSE
)
}
\arguments{
\item{plots}{A list of \code{ggplot} objects from \code{\link[=de_volcano]{de_volcano()}}.}

\item{labels}{Character vector of panel titles; defaults to \code{names(plots)}.}

\item{legend_position}{Where the collected legend goes.}

\item{keep_first_caption}{Logical. Keep the first panel's caption under that
panel instead of promoting it to a single figure-level caption.}
}
\value{
A \code{patchwork} object.
}
\description{
Puts several volcanoes side by side on common limits with one collected
legend. Intended for \code{orientation = "vertical"} panels, where the fold-change
axis runs top-to-bottom and reads across a row.
}
\examples{
de <- data.frame(
  logFC     = c(-3, -1, 0.2, 1.5, 4),
  P.Value   = c(1e-6, 0.02, 0.7, 0.01, 1e-8),
  adj.P.Val = c(1e-4, 0.06, 0.9, 0.04, 1e-6),
  row.names = paste0("Gene", 1:5)
)
if (requireNamespace("patchwork", quietly = TRUE)) {
  p <- de_volcano(de, fc_cutoff = 1, orientation = "vertical")
  de_volcano_grid(list(A = p, B = p))
}
}
