% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-io.R
\name{write_session_provenance}
\alias{write_session_provenance}
\title{Record session provenance to a text file}
\usage{
write_session_provenance(path, genome_build = NULL, ensembl_version = NULL)
}
\arguments{
\item{path}{Output file path. Parent directories are created.}

\item{genome_build}{Character genome build, e.g. \code{"mm10"}, or \code{NULL}.}

\item{ensembl_version}{Ensembl release passed to \code{useEnsembl()}, or \code{NULL}
for the floating current release.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes the genome build, the requested Ensembl release, the \strong{resolved}
biomaRt archive release, the bulkiRNA and hard-dependency versions, resolved
reference-data snapshots, and the full \code{sessionInfo()}. The resolved archive
matters because \code{sessionInfo()} records only the biomaRt \emph{package} version --
not which remote Ensembl release the annotation actually came from, which is
the thing that changes an analysis underneath you.
}
\examples{
f <- tempfile(fileext = ".txt")
write_session_provenance(f, genome_build = "mm10")
length(readLines(f)) > 0
}
