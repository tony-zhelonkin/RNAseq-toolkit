% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsdb-file.R
\name{gsdb_from_file}
\alias{gsdb_from_file}
\title{Read gene sets from a GMT or GMX file}
\usage{
gsdb_from_file(
  path,
  database = NULL,
  species = "Mus musculus",
  prefix = NULL,
  min_size = NULL,
  max_size = NULL,
  verbose = FALSE
)
}
\arguments{
\item{path}{Character(1) path to a GMT or GMX file.}

\item{database}{Character(1) provider label; defaults to the file's base
name.}

\item{species}{Character(1) species the file's symbols belong to.}

\item{prefix}{Character(1) or \code{NULL}; prepended to every set id as
\code{paste0(prefix, "_", id)}.}

\item{min_size, max_size}{Integer(1) or \code{NULL}; drop sets outside these
bounds.}

\item{verbose}{Logical(1); message what was parsed.}
}
\value{
A \code{\link[=gs_db]{gs_db()}}.
}
\description{
One reader for both MSigDB text formats; the layout is sniffed rather than
declared. A \code{.gmt}/\code{.gmx} extension decides it outright; otherwise the file
is inspected — GMX has a fixed column count in its first two rows (row 1
descriptions, row 2 set names, rows 3+ one gene per set per row), while GMT
rows are independent sets of the form
\verb{name<TAB>description<TAB>gene1<TAB>...}.
}
\examples{
gmt <- tempfile(fileext = ".gmt")
writeLines(c("SET_A\tfirst set\tGene1\tGene2",
             "SET_B\tsecond set\tGene2\tGene3"), gmt)
gsdb_from_file(gmt, species = "Mus musculus")
}
