% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-ranks.R
\name{gs_ranks}
\alias{gs_ranks}
\title{Build a ranked gene vector for gene-set testing}
\usage{
gs_ranks(
  x,
  metric = "t",
  genes = NULL,
  collapse = c("none", "max_abs", "mean")
)
}
\arguments{
\item{x}{A data frame of DE results, or an already-named numeric vector.}

\item{metric}{Character. Column of \code{x} to rank by. Default \code{"t"}.}

\item{genes}{Optional character vector of gene identifiers, or the name of a
column of \code{x} holding them. Defaults to \code{rownames(x)}.}

\item{collapse}{How to resolve duplicated gene identifiers: \code{"max_abs"}
keeps the row with the largest absolute metric, \code{"mean"} averages, and
\code{"none"} (default) leaves duplicates in place, matching the legacy
\code{run_gsea()} behaviour.}
}
\value{
A named numeric vector, sorted decreasing.
}
\description{
Turns a differential-expression table into the named, decreasing-sorted
numeric vector that \code{\link[=gs_test]{gs_test()}} feeds to fgsea. This is the whole of the old
\code{run_gsea()} preamble, split out so ranking is inspectable and testable
independently of the test itself.
}
\details{
Missing and infinite values are dropped with a warning, because a rank
vector containing them is silently truncated by fgsea anyway.
}
\examples{
de <- data.frame(t = c(3, -1, 2), row.names = c("A", "B", "C"))
gs_ranks(de)
}
