% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated-gs.R, R/gs-db.R
\name{filter_by_size}
\alias{filter_by_size}
\title{Deprecated: filter a T2G/T2N pair by gene-set size}
\usage{
filter_by_size(db, min_size = NULL, max_size = NULL, verbose = FALSE)

filter_by_size(db, min_size = NULL, max_size = NULL, verbose = FALSE)
}
\arguments{
\item{db}{A \code{gs_db}.}

\item{min_size}{Integer(1) or \code{NULL}; smallest set size to keep.}

\item{max_size}{Integer(1) or \code{NULL}; largest set size to keep.}

\item{verbose}{Logical(1); message how many sets were dropped.}

\item{result}{List with \code{T2G} (\code{gs_name}, \code{gene_symbol}) and \code{T2N}
(\code{gs_name}, \code{description}).}
}
\value{
List with filtered \code{T2G} and \code{T2N}, same shape as \code{result}.

A \code{gs_db} with out-of-range sets removed.
}
\description{
Superseded by the internal \code{filter_by_size()} in \code{R/gs-db.R}, which
operates on a \code{\link[=gs_db]{gs_db()}} rather than a \code{list(T2G =, T2N =)} pair. The 5/500
defaults live here; the internal version defaults to \code{NULL}/\code{NULL} ("no
bound").

The size filter that used to live in \code{build_reference_databases.R}. \code{NULL}
bounds mean "no bound".
}
\details{
\strong{Naming collision, not resolved here:} \code{R/gs-db.R} already defines an
internal (non-exported, but still top-level) \code{filter_by_size(db, min_size = NULL, max_size = NULL, verbose = FALSE)}. A package namespace has one
binding per name, so this shim's own \code{filter_by_size(result, min_size = 5, max_size = 500)} would silently shadow -- or be shadowed by -- that
internal one purely based on file collation order, corrupting whichever
one loses. This implementation therefore does \strong{not} call the internal
\code{filter_by_size()} at all; it reimplements the old
\code{build_reference_databases.R} size filter directly on the \code{T2G}/\code{T2N}
data frames. The collision itself (two distinct top-level \verb{filter_by_size <- function(...)} bindings in one package) is a package-level defect that
this shim cannot fix without editing \code{R/gs-db.R}; see the handback report.
}
\keyword{internal}
